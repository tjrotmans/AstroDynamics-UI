/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/api/health": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Health check */
        get: operations["health"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/bodies": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Body catalog — all supported target bodies with physics properties */
        get: operations["getBodies"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/bodies/{name}/state": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Heliocentric state vector for a catalog body at an epoch
         * @description Only ANISE/DE440S-covered bodies (Mercury, Venus, Earth, Moon, Mars, Jupiter, Saturn) return a real state — small bodies in the catalog (Bennu, Apophis, Ryugu, Eros, Didymos,...) have no fixed ephemeris and return 422, since their position is only known via a mission's own [target_body.keplerian_orbit], which this generic endpoint has no access to. Known gap: querying "Sun" also 422s today even though DE440S covers it — anise_body in design.rs has no "sun" match arm yet.
         */
        get: operations["getBodyState"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/hardware": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Hardware catalog — reaction wheels, thrusters, star trackers, OpNav cameras */
        get: operations["getHardware"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/objectives": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Supported mission objectives */
        get: operations["getObjectives"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/presets": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Example mission config library — returns all preset MissionConfigs as JSON
         * @description Each preset is compiled from the example TOML files in MissionPlanner/config/ and returned with a derived id, display name, and one-line description. The config field is a full MissionConfig object that can be POSTed directly to /api/design/trajectory or /api/simulate.
         */
        get: operations["getPresets"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/validate": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Validate a mission config — returns errors + structured summary */
        post: operations["validateConfig"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/design/trajectory": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Run trajectory solver — Hohmann, Lambert porkchop, DiffCorrection, MonteCarlo, GA, or PSO */
        post: operations["designTrajectory"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/design/gnc": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Run GNC sizing — selects hardware, sizes EKF, computes disturbance torques */
        post: operations["designGnc"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/design/vehicle": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Derived spacecraft mass properties (total mass, center of mass, full inertia tensor, per-component breakdown) and, optionally, per-plate SRP force vectors —
         * @description Pure computation, synchronous — no job/polling (cheap: closed-form bus-box + point-mass parallel-axis, no propagation). `com_m` and `inertia_kgm2` are computed about the spacecraft's TRUE center of mass; every position field elsewhere in MissionConfig (HardwareItem placement fields) is relative to the FIXED geometric-center origin (the bus centroid), NOT the CoM — see docs/MP/MANUAL.md §2.3 for the frame convention this enforces. inertia_kgm2 is a full symmetric 3x3 tensor (row-major) and is generally NOT diagonal once any component sits off-axis — do not assume only the 3 diagonal entries are meaningful. Set sun_hat_body to also get the srp field (per-plate force vectors from the real orbital_models flat-plate law, for live SRP-arrow rendering); omit it (or send null) to skip that section and get mass properties only.
         */
        post: operations["designVehicle"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/design/slew-test": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Cheap synchronous canned attitude-step test —
         * @description Runs the same closed-loop machinery src/bin/sixdof_control_demo.rs verifies by hand (quaternion PD control law + mode-dependent control allocation, Phase 13d/13e) against a FIXED reference scenario (a circular Earth orbit at 700 km altitude, not user-configurable), driven by the real vehicle/actuator/gain values derived from the submitted config -- the same helpers run_streaming itself uses (build_spacecraft_properties, wheel_cluster_from_hardware, rcs_from_hardware, pd_gains_from_cfg). Synchronous, no job/polling: duration_s/tick_s are clamped server-side (tick 0.1-60 s, duration up to 7200 s) to keep wall-clock cost bounded. Use this for fast gain-tuning/actuator-sizing iteration before committing to a full /api/simulate run. See docs/MP/MANUAL.md section 7 for the governing control law/allocation math.
         */
        post: operations["designSlewTest"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/simulate": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Start an async high-fidelity simulation job. Returns immediately with a job_id; the job runs in the background.
         * @description Branches on MissionConfig.cruise_seed FIRST, then simulation.monte_carlo_runs: cruise_seed set AND monte_carlo_runs > 0 (the design notes backlog item #10) starts a cruise Monte Carlo job -- disperses vehicle mass/inertia/reflectivity (cruise.rs::run_cruise_mc_streaming) across monte_carlo_runs cruise-loop runs; /stream carries CruiseMcRunMsg per *completed run* (never per-tick), /result returns CruiseMcSummaryResult, /steps returns {"runs": CruiseMcRunMsg[]}. cruise_seed set alone (monte_carlo_runs == 0, Phase 5.2) starts a single cruise-loop job — the Phase 13 translation+attitude+guidance composition (cruise.rs::run_cruise_leg) flown against a real Layer-1 reference trajectory, instead of the body-centric proximity-ops path; /stream carries CruiseStepMsg per control tick, /result returns CruiseResult, /steps returns {"steps": CruiseStepMsg[]}. Otherwise (cruise_seed not set): monte_carlo_runs == 0 (default) starts a single-run body-centric job — /stream carries SimStepMsg per measurement step, /result returns SimResult. monte_carlo_runs > 0 starts a body-centric Monte Carlo job instead — per the Monte Carlo streaming rule, this NEVER gets the per-step feed; /stream carries one McRunMsg per *completed run* (with a sparse trajectory only on a stride-selected subset, capped at 20 runs regardless of monte_carlo_runs), /result returns McSummaryResult, /steps returns {"runs": McRunMsg[]} instead of {"steps": SimStepMsg[]}. Same job_id/status/stream/steps/result endpoints serve all four kinds — the client already knows which kind it started. Phase 01/02 consistency: for either cruise job kind, this endpoint now integrates at the SAME simulation.rtol/atol Phase 01's own trajectory design used (previously hardcoded to 1e-10/1e-12 inside the cruise loop regardless of what was configured), and auto-populates cruise_seed.body_tracks with real ANISE-queried tracks for any target_body.third_bodies name not already present in the request (requires trajectory.departure_epoch to be set; silently adds nothing otherwise — best-effort, never an error) — so Phase 01's configured third-body perturbers carry over into the cruise simulation instead of silently disappearing.
         */
        post: operations["startSimulate"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/simulate/{job_id}/status": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Poll job progress */
        get: operations["getSimulateStatus"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/simulate/{job_id}/stream": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** WebSocket upgrade — live feed, shape depends on job kind. Cruise MC jobs (cruise_seed set + monte_carlo_runs > 0): CruiseMcRunMsg per completed run. Cruise jobs (cruise_seed set alone): CruiseStepMsg per control tick. Single-run jobs: SimStepMsg per measurement step (dt_meas_s, not the denser dt_truth_s). Body-centric Monte Carlo jobs (monte_carlo_runs > 0): McRunMsg per completed run, never per truth-step. IMPORTANT, added after a real client crash: which of these four shapes a given job's stream carries is decided ENTIRELY by the request that started it (cruise_seed presence and monte_carlo_runs' value) — get that combination wrong (e.g. set cruise_seed and accidentally leave monte_carlo_runs at a stale nonzero default) and you'll get a completely different, unexpected shape back with no error. CruiseStepMsg/CruiseMcRunMsg both now carry a msg_type discriminant ("cruise_tick"/"cruise_mc_run") specifically so a client can check it before trusting the rest of a parsed frame, instead of assuming the shape matches the request it thought it made. Ends with a literal text frame "__DONE__" (not a clean WS close) — if the job already finished before the client connects, "__DONE__" is sent immediately rather than hanging (broadcast channels don't replay history to late subscribers, and fast sims often finish before the WS handshake completes). */
        get: operations["streamSimulate"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/simulate/{job_id}/steps": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Full step/run history for a job, partial if still running. For replaying a run whose WS handshake didn't complete before the simulation finished (common for fast sims) — broadcast channels don't replay history to late subscribers, so the live stream alone can't recover this.
         * @description Shape depends on which kind of job job_id refers to: a cruise job (started with cruise_seed set, monte_carlo_runs == 0) or a single-run job both return {"steps": CruiseStepMsg[] | SimStepMsg[]}; a cruise MC job (cruise_seed set + monte_carlo_runs > 0) or a body-centric Monte Carlo job (monte_carlo_runs > 0 alone) both return {"runs": CruiseMcRunMsg[] | McRunMsg[]} instead.
         */
        get: operations["getSimulateSteps"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/simulate/{job_id}/result": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Final summary, only available once the job is done
         * @description Returns CruiseMcSummaryResult for a cruise MC job (cruise_seed set + monte_carlo_runs > 0), CruiseResult for a cruise job (cruise_seed set alone), SimResult for a single-run job, or McSummaryResult for a body-centric Monte Carlo job (monte_carlo_runs > 0 alone).
         */
        get: operations["getSimulateResult"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/simulate/{job_id}/cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Request early stop for a running job (Phase 9k task 4). Idempotent — cancelling an already-finished job is a harmless no-op, not an error.
         * @description For a single-run job (monte_carlo_runs == 0), the truth-integration loop polls the cancellation flag every truth step (dt_truth_s), so this takes effect within roughly one truth step; /result then returns whatever telemetry was collected up to that point, not an error. For a Monte Carlo job (monte_carlo_runs > 0) this only affects /status reporting — the already-dispatched batch of runs is not interruptible with the current thread::scope-based implementation, a known and documented limitation, not an oversight.
         */
        post: operations["cancelSimulate"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/optimize": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Start an async Phase 9 trajectory optimization job (real propagated dynamics — GA/PSO fitness includes actual numerical propagation under [optimization.force_model], NOT the closed-form Lambert proxy used by /api/design/trajectory's GA/PSO). Requires the config's `optimization` field to be set (non-null) — a config without it is rejected with 422. */
        post: operations["startOptimize"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/optimize/{job_id}/status": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Poll job progress. Unlike /api/simulate's status, total_steps is usually a known integer here (GA generations or PSO iterations come straight from the config), not always null. */
        get: operations["getOptimizeStatus"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/optimize/{job_id}/stream": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** WebSocket upgrade — live OptimizeStepMsg feed, one message per generation (GA) or iteration (PSO). Never carries a propagated arc per step (only the final /result does) — see the design notes Monte Carlo streaming rule, applied here too: an expensive value (a re-propagated arc) is computed once, not on every step. For MGA jobs, an MgaSequenceContextMsg frame (Phase 9k "step-stream context") is also interleaved — one per candidate flyby-body sequence, sent BEFORE that sequence's own OptimizeStepMsg frames. Ends with a literal text frame "__DONE__", same late-subscriber handling as /api/simulate/{job_id}/stream. */
        get: operations["streamOptimize"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/optimize/{job_id}/result": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Final result, only available once the job is done */
        get: operations["getOptimizeResult"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/optimize/{job_id}/cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Request early stop for a running job (Phase 9k task 4). Idempotent — cancelling an already-finished job is a harmless no-op, not an error.
         * @description Honored by GA/PSO (checked every generation/iteration and every refinement pass) — the job stops within roughly one generation and /result returns whatever candidate was best so far. NOT honored by MGA (method = "MGA") — cancelled is still recorded and reported by /status, but the DE search has no cancellation hook yet and runs to completion regardless.
         */
        post: operations["cancelOptimize"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mga-sequence-search": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Run the Tisserand beam search only, and return every ranked candidate flyby sequence. Plain synchronous request/response — no job_id, no polling.
         * @description Wraps crate::sequence_search::run_sequence_search directly, without running the (much more expensive) ballistic grid scan or DE/MBH search that normally follow it in the MGA pipeline. Requires [optimization.mga.sequence_search] (candidate_bodies/max_legs/beam_width) to be present — [optimization.mga.flyby_bodies] is not used here, since the whole point of this endpoint is discovering flyby_bodies automatically. Fast enough (no propagation or Lambert solve per candidate) that a plain synchronous response is correct, unlike /api/mga-scan and /api/optimize which need the async-job pattern. Lets a caller see the full ranked field — not just the single top pick — before committing to the expensive full scan or DE search on one of them.
         */
        post: operations["searchMgaSequence"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mga-scan": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Start an async Phase 9w ballistic MGA window-scan job (deterministic, propagation-free — powered-flyby Lambert legs, no DSMs). Returns immediately with a job_id.
         * @description Wraps the mga-scan CLI (crate::mga_scan_run::mga_scan_api) — same async-job pattern as /api/optimize/simulate. Requires [optimization] with a fixed [optimization.mga] leg sequence (flyby_bodies, not sequence_search — run search-sequence/the Tisserand beam search first to pick a sequence for an auto-discovery mission) and an [optimization.mga.scan] section. A scan can take from seconds to several minutes depending on grid density (a real 4-leg VEEGA scan at 1,949 departure dates x 18 TOF points/leg took ~6 minutes). No WebSocket stream is offered — run_mga_scan has no per-evaluation progress hook, unlike GA/PSO/MGA-DE's generation loop, so there's nothing meaningful to stream; /status only reports running/done/error.
         */
        post: operations["startMgaScan"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mga-scan/{job_id}/status": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Poll job progress. No step counter (see /api/mga-scan's description) — running/error only. */
        get: operations["getMgaScanStatus"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/mga-scan/{job_id}/result": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Final result, only available once the job is done. Same fields mga_window_scan.csv already has, as a JSON array — also still written to disk as a side effect (mission's simulation.output_dir), same as every other async-job endpoint. */
        get: operations["getMgaScanResult"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        HealthResponse: {
            ok: boolean;
        };
        ErrorResponse: {
            error: string;
        };
        ErrorsResponse: {
            errors: string[];
        };
        BodyInfo: {
            name: string;
            /** @description Body classification for frontend grouping: "Planet", "Moon", "Asteroid", "Star" */
            kind: string;
            /** @description True if this body has real ephemeris coverage via GET /api/bodies/{name}/state. Always true for major bodies (Sun, Mercury, Venus, Earth, Moon, Mars, Jupiter, Saturn) via the always-present de440s.bsp. True for Phobos, Deimos, Europa, and Titan only if this server also has the relevant satellite kernel loaded (mar099s.bsp / jup365.bsp / sat441.bsp) -- check this field rather than assuming, since it's deployment-dependent. Always false for other small bodies (Bennu, Apophis, Ryugu, Eros, Didymos,...), which have no DE440S-family kernel at all. */
            anise_covered: boolean;
            mu_m3s2: number;
            radius_m: number;
            /** @description PointMass | J2 | J2J3J4 | SphericalHarmonic */
            gravity_model: string;
            /** @description None | Exponential | NRLMSISE */
            atmosphere: string;
            j2?: number | null;
            j3?: number | null;
            j4?: number | null;
            spin_rate_rads?: number | null;
            /** @description North pole right ascension [deg], ICRF/J2000 constant term. Null if no citable pole orientation is available (e.g. small bodies) — central-body gravity fidelity falls back to point-mass for those. */
            pole_ra_deg?: number | null;
            /** @description North pole declination [deg], ICRF/J2000 constant term. */
            pole_dec_deg?: number | null;
            /** @description The body this one actually orbits. Null means it orbits the Sun directly (true for every planet/asteroid). e.g. "Earth" for the Moon, "Jupiter" for Europa. */
            primary?: string | null;
            /** @description Mean orbital semi-major axis around `primary` (or the Sun when primary is null) [m]. Null for the Sun and for small bodies whose full elements live in orbital_elements instead. Source: JPL Williams 2021 (planets) / JPL SSD satellite parameters (moons). */
            sma_m?: number | null;
            /** @description Full HELIOCENTRIC Keplerian elements at a stated epoch (— real catalog-served orbit shapes so clients can draw true elliptical rings, including for small bodies with no ephemeris coverage, instead of hardcoding a client-side element table). Present for the 8 planets (Standish 1992 J2000 mean elements) and the 5 catalogued small bodies (JPL SBDB osculating elements at each body's stated epoch). Null for the Sun (frame origin) and for every moon — a moon's elements are primary-relative, a deliberately different contract not overloaded onto this heliocentric field. */
            orbital_elements?: components["schemas"]["OrbitalElements"] | null;
        };
        /** @description Heliocentric Keplerian orbital elements at epoch_jd, referred to the MEAN ECLIPTIC AND EQUINOX OF J2000 (the native frame of both source tables) — NOT the equatorial ICRF frame /api/bodies/{name}/state vectors use; mixing the two requires rotating by the J2000 obliquity (23.43929111 deg). Mean/osculating elements at one epoch with no secular rates: intended for orbit rings and rough position estimates, not a substitute for real ephemeris where it exists. */
        OrbitalElements: {
            /** @description Semi-major axis [m] */
            sma_m: number;
            eccentricity: number;
            /** @description Inclination to the J2000 ecliptic [deg] */
            inclination_deg: number;
            /** @description Longitude of the ascending node [deg] */
            raan_deg: number;
            /** @description Argument of periapsis [deg] */
            arg_periapsis_deg: number;
            /** @description Mean anomaly at epoch_jd [deg] */
            mean_anomaly_deg: number;
            /** @description Epoch of the elements [Julian Date, TDB]. 2451545.0 (J2000.0) for planets; the JPL SBDB solution epoch for small bodies. */
            epoch_jd: number;
        };
        BodiesResponse: {
            bodies: components["schemas"]["BodyInfo"][];
        };
        PresetEntry: {
            /** @description Kebab-case identifier matching the source TOML filename (without.toml) */
            id: string;
            /** @description Display name from [mission].name in the preset TOML */
            name: string;
            /** @description Derived one-liner: '{objective} — {departure_body} → {target} ({solver})' */
            description: string;
            /** @description Full MissionConfig parsed from the preset TOML. Can be POSTed directly to /api/design/trajectory or /api/simulate. */
            config: unknown;
        };
        PresetsResponse: {
            presets: components["schemas"]["PresetEntry"][];
        };
        BodyStateResponse: {
            name: string;
            epoch: string;
            x_m: number;
            y_m: number;
            z_m: number;
            vx_mps: number;
            vy_mps: number;
            vz_mps: number;
        };
        ReactionWheelSpec: {
            name: string;
            mass_kg: number;
            max_torque_nm: number;
            max_momentum_nms: number;
            max_speed_rads: number;
            inertia_kgm2: number;
            /** @description Nominal operating power draw [W] — representative class estimate, not a specific flight unit's datasheet value. */
            power_w: number;
        };
        ThrusterSpec: {
            name: string;
            thrust_n: number;
            isp_s: number;
            /** @description Minimum impulse-bit duration [s] */
            min_pulse_s: number;
            /** @description Nominal valve-driver power draw [W] — representative class estimate. */
            power_w: number;
        };
        StarTrackerSpec: {
            name: string;
            noise_rad: number;
            mass_kg: number;
            /** @description Nominal operating power draw [W] — representative class estimate. */
            power_w: number;
        };
        /** @description Catalog grade — units are [rad], NOT [mrad]. Contrast with HardwareItem's OpNavCamera variant (bearing_noise_mrad/angular_size_noise_mrad), which is in mrad. This unit mismatch is real, not a typo — see hardware_catalog/src/sensor.rs vs MissionPlanner/src/config.rs. */
        OpNavCameraSpec: {
            name: string;
            /** @description 1-sigma bearing noise [rad] */
            bearing_noise_rad: number;
            /** @description 1-sigma angular size noise [rad] */
            angular_size_noise_rad: number;
            mass_kg: number;
            /** @description Nominal operating power draw [W] — representative class estimate. */
            power_w: number;
        };
        /** @description IMU/accelerometer grade — ΔV measurement noise during impulsive manoeuvres. Medium grade matches the Bennu mission's IMU_DV_SIGMA_MPS constant. */
        ImuSpec: {
            name: string;
            /** @description 1-sigma ΔV noise per axis [m/s] */
            dv_noise_mps: number;
            mass_kg: number;
            /** @description Nominal operating power draw [W] — representative class estimate. */
            power_w: number;
        };
        /** @description LIDAR altimeter grade — slant-range measurement to the target body's surface. Medium grade matches the Bennu mission's LIDAR_SIGMA_M/LIDAR_MAX_RANGE_M constants. */
        LidarSpec: {
            name: string;
            /** @description 1-sigma range noise [m] */
            range_noise_m: number;
            /** @description Maximum operational range [m] — no return beyond this. */
            max_range_m: number;
            mass_kg: number;
            /** @description Nominal operating power draw [W] — representative class estimate. */
            power_w: number;
        };
        /** @description Landmark-tracking (stereophotoclinometry-analogue) sensor grade — bearing-only line-of-sight measurements to catalogued surface features, giving full 3-D position from several simultaneous sightings without LIDAR. Distinct catalog item from OpNavCameraSpec despite similar optics, since it's sized against a different requirement (multi-landmark 3-D position knowledge). */
        LandmarkSensorSpec: {
            name: string;
            /** @description 1-sigma per-landmark bearing noise [rad] */
            bearing_noise_rad: number;
            /** @description Number of catalogued landmarks the processing pipeline can track. */
            catalog_size: number;
            mass_kg: number;
            /** @description Nominal operating power draw [W] — representative class estimate. */
            power_w: number;
        };
        /** @description Deep Space Network ground-link grade — heliocentric uplink accuracy from ground orbit determination. Mass/power describe the onboard transponder + HGA terminal, not a ground asset. */
        DsnLinkSpec: {
            name: string;
            /** @description 1-sigma two-way range noise [m] */
            range_noise_m: number;
            /** @description 1-sigma range-rate (Doppler) noise [m/s] */
            range_rate_noise_mps: number;
            /** @description 1-sigma Delta-DOR transverse angular noise [rad] */
            ddor_noise_rad: number;
            mass_kg: number;
            /** @description Nominal operating power draw [W] — representative class estimate. */
            power_w: number;
        };
        /** @description One verified (C3, injected mass) data point from a real flown mission — see hardware_catalog/src/launch_vehicle.rs for citations. NOT a smoothed vendor curve. */
        LaunchVehiclePerformancePoint: {
            c3_km2s2: number;
            injected_mass_kg: number;
        };
        /** @description A launch vehicle's real C3-vs-injected-mass performance, built from one or more verified flown-mission data points, sorted by C3 ascending. Empty performance_points (Starship) means no real interplanetary payload has flown yet — not missing data to be estimated. */
        LaunchVehicleSpec: {
            /** @enum {string} */
            name: "Falcon9" | "Starship" | "AtlasV401" | "AtlasV551Star48";
            performance_points: components["schemas"]["LaunchVehiclePerformancePoint"][];
        };
        /** @description Feasibility of the configured launch vehicle ([spacecraft].launch_vehicle) delivering a trajectory's required departure C3 at the spacecraft's wet mass — the structured, API-facing form of what used to be CLI-only println! output. Added see the design notes. Only ever present (non-null at the parent field) for an Earth departure with a real, catalog-recognized vehicle configured — every other case (non-Earth departure, no vehicle configured, unrecognized name) is null at the parent field rather than a variant of this object, since there's no real vehicle to report a feasibility number for. */
        LaunchVehicleCheckApiResult: {
            /** @description Catalog name of the configured vehicle. */
            vehicle: string;
            /** @description This trajectory's required departure C3 [km²/s²]. */
            required_c3_km2s2: number;
            /** @description Max mass [kg] the vehicle can inject at required_c3_km2s2, from its real flown-mission curve. Null if required_c3_km2s2 exceeds every verified data point for this vehicle (no extrapolation). */
            max_injected_mass_kg?: number | null;
            /** @description Spacecraft wet mass [kg] ([spacecraft].mass_kg), for comparison. */
            spacecraft_mass_kg: number;
            /** @description max_injected_mass_kg - spacecraft_mass_kg. Null whenever max_injected_mass_kg is null. */
            margin_kg?: number | null;
            /** @description True only when the vehicle has verified performance data at this C3 AND can lift this wet mass. */
            feasible: boolean;
            /** @description Partial coverage (Phase 14e): the maximum C3 [km²/s²] the vehicle gives THIS wet mass, from the inverse of its verified curve (clamped to the verified range — a light payload above the highest verified point reports that point's C3). Null when no verified point shows the vehicle lifting this mass to any escape energy. */
            max_c3_at_mass_km2s2?: number | null;
            /** @description Parking-orbit radius [m] the ΔV split below is referred to ([trajectory.departure].parking_orbit_radius_m, or the atmospheric/airless default). */
            parking_orbit_radius_m?: number;
            /** @description Departure ΔV [m/s] the launcher delivers from that parking orbit: √(C3_L + 2μ/r_p) − √(μ/r_p) with C3_L = min(max_c3_at_mass_km2s2, required_c3_km2s2). The full tangential escape burn when the launcher covers the C3; 0 when it cannot lift the mass at all. */
            launcher_departure_dv_ms?: number;
            /** @description Perigee top-up [m/s] the spacecraft adds from the same periapsis: √(C3_req + 2μ/r_p) − √(C3_L + 2μ/r_p). 0 when the launcher covers the C3; the whole escape burn when it cannot lift the mass. */
            onboard_departure_dv_ms?: number;
            /** @description Why feasible is false, when it is: distinguishes 'required C3 exceeds the vehicle's verified performance range' from 'the vehicle cannot lift this wet mass at the required C3' (and 'no verified interplanetary performance data' for a vehicle with an empty curve). Null when feasible. */
            reason?: string | null;
        };
        /** @description Closed-form launch geometry from a departure asymptote (Phase 14c, trajectory_solver::launch_geometry; MANUAL.md §13.6). For fixed v∞ and periapsis the escape hyperbola is a one-parameter family (rotation about the asymptote), so the plane is chosen after the interplanetary solve to satisfy the site: i ≥ |DLA| (contains the asymptote) and i ≥ |site latitude| (no dogleg). No ascent is modelled — the parking-orbit coast and injection state are physical, the ascent a frontend schematic. Angles in degrees; vectors body-relative inertial (ICRF/J2000). */
        LaunchGeometryApiResult: {
            /** @description [trajectory.departure.launch_site].name. */
            site_name: string;
            /** @description Site geodetic latitude [deg], positive north — the only site quantity in the physics. */
            site_lat_deg: number;
            /** @description Site longitude [deg], positive east — display / future daily-window use. */
            site_lon_deg: number;
            /** @description Launch energy C3 = v∞² [km²/s²]. */
            c3_km2s2: number;
            /** @description Right ascension of the launch asymptote [deg], body equatorial frame, [0, 360). */
            rla_deg: number;
            /** @description Declination of the launch asymptote [deg], [−90, 90]. */
            dla_deg: number;
            /** @description Inclination of the parking/escape plane to the body equator [deg] — the configured [trajectory.departure].inclination_deg, or the minimum feasible one. */
            inclination_deg: number;
            /** @description max(|DLA|, |site latitude|) [deg] — the cheapest plane that contains the asymptote and needs no dogleg. */
            min_inclination_deg: number;
            /** @description inclination_deg ≥ min_inclination_deg. False = the requested plane needs a dogleg from this site; reported infeasible, never priced. */
            feasible_no_dogleg: boolean;
            /** @description Launch azimuth from north [deg] for the ascending (north-east-going) pass: sin β = cos i / cos φ. The descending alternative is 180 − β. */
            launch_azimuth_deg: number;
            /** @description Right ascension of the parking orbit's ascending node [deg], body equatorial frame. */
            raan_deg: number;
            /** @description In-plane angle coasted in the parking orbit from the site's ascending crossing to the injection periapsis [deg]. Schematic: the ascent's own downrange angle is not modelled. */
            coast_angle_deg: number;
            /** @description Parking / injection radius [m] (185 km default altitude in Launch mode). */
            parking_orbit_radius_m: number;
            /** @description Injection point (periapsis of the escape hyperbola), body-relative inertial [m]. */
            injection_r_m: number[];
            /** @description Velocity after the injection burn, body-relative inertial [m/s]; |v|² − 2μ/r_p = v∞² exactly. */
            injection_v_mps: number[];
            /** @description Parking-orbit circular velocity at the injection point before the burn [m/s]. */
            parking_v_mps: number[];
            /** @description Tangential injection burn √(v∞² + 2μ/r_p) − √(μ/r_p) [m/s]. */
            dv_injection_ms: number;
        };
        /** @description Two-pool ΔV ledger + propellant feasibility for one trajectory (Phase 14e MANUAL.md §13.5). Every ΔV is paid from the LAUNCHER pool (the upper stage's injection, priced against the vehicle's verified C3-vs-mass curve — Earth departure with a catalog vehicle only) or the ONBOARD pool (Isp·g₀·ln(m_wet/m_dry): the departure remainder, every DSM, and ALWAYS the arrival burn). Partial launcher coverage is real: a vehicle short of the required C3 still delivers its maximum at this mass and the spacecraft tops up from the same periapsis (see LaunchVehicleCheckApiResult). Computed by ONE shared helper for every result type, so BestArcApiResult/OptimizerApiResult/OptimizeApiResult ledgers are directly comparable. */
        DvLedgerApiResult: {
            /** @description Full departure burn [m/s] from the parking orbit before any split (the tangential escape burn for the narrowing stage and MGA; the search's own, generally non-tangential, dv_departure_ms for single-leg GA/PSO). */
            departure_dv_ms: number;
            /**
             * @description Who pays the departure burn: fully the launcher, fully onboard (no launcher share at all), or split (launcher at its maximum C3 for this mass, spacecraft tops up).
             * @enum {string}
             */
            departure_dv_pool: "launcher" | "onboard" | "split";
            /** @description Departure ΔV [m/s] delivered by the launch vehicle — 0 without one. */
            launcher_dv_ms: number;
            /** @description Departure ΔV [m/s] the spacecraft's own engine must add. */
            onboard_departure_dv_ms: number;
            /** @description Arrival/capture burn [m/s] — always onboard; 0 for a Flyby. */
            onboard_arrival_dv_ms: number;
            /** @description Deep-space maneuvers [m/s] (sum of mga_dv_dsms_ms for MGA) — always onboard; 0 for a single-leg transfer. */
            onboard_dsm_dv_ms: number;
            /** @description onboard_departure_dv_ms + onboard_dsm_dv_ms + onboard_arrival_dv_ms. */
            onboard_dv_required_ms: number;
            /** @description Isp·g₀·ln(m_wet/m_dry) [m/s]. Null when no [spacecraft.propulsion] is configured (the three fields below are then null too). */
            onboard_dv_available_ms?: number | null;
            /** @description Propellant the onboard requirement consumes, m_wet·(1 − e^(−Δv/(Isp·g₀))) [kg] — compare with [spacecraft].propellant_mass_kg. */
            onboard_propellant_required_kg?: number | null;
            /** @description m_wet/m_dry the onboard requirement demands, e^(Δv/(Isp·g₀)) — a scale-free feasibility figure (chemical stages top out around 10–15). */
            mass_ratio_required?: number | null;
            /** @description onboard_dv_available_ms >= onboard_dv_required_ms. */
            propellant_feasible?: boolean | null;
            /** @description onboard_dv_available_ms − onboard_dv_required_ms [m/s]; positive = onboard margin remaining. The same number the parent's legacy budget_margin_ms carries. */
            budget_margin_ms?: number | null;
        };
        HardwareResponse: {
            reaction_wheels: components["schemas"]["ReactionWheelSpec"][];
            thrusters: components["schemas"]["ThrusterSpec"][];
            star_trackers: components["schemas"]["StarTrackerSpec"][];
            opnav_cameras: components["schemas"]["OpNavCameraSpec"][];
            imus: components["schemas"]["ImuSpec"][];
            lidars: components["schemas"]["LidarSpec"][];
            landmark_sensors: components["schemas"]["LandmarkSensorSpec"][];
            dsn_links: components["schemas"]["DsnLinkSpec"][];
            /** @description Added. Only meaningful for an Earth departure ([trajectory].departure_body) — see LaunchVehicleCheckApiResult. */
            launch_vehicles: components["schemas"]["LaunchVehicleSpec"][];
        };
        MissionObjective: {
            /** @enum {string} */
            id: "Flyby" | "Orbit" | "Landing" | "Rendezvous" | "SampleReturn";
            description: string;
        };
        ObjectivesResponse: {
            objectives: components["schemas"]["MissionObjective"][];
        };
        KeplerianOrbit: {
            sma_au: number;
            eccentricity: number;
            inclination_deg: number;
            raan_deg: number;
            aop_deg: number;
            mean_anomaly_deg: number;
            epoch_jd: number;
        };
        TargetBodyConfig: {
            /** @description Body name — looked up in catalog; fields below override catalog values */
            name: string;
            mu_m3s2?: number | null;
            radius_m?: number | null;
            /** @enum {string|null} */
            gravity_model?: "PointMass" | "J2" | "J2J3J4" | "SphericalHarmonic" | null;
            /** @enum {string|null} */
            atmosphere?: "None" | "Exponential" | "NRLMSISE" | null;
            /**
             * @description The accepted tag is "Anise" (PascalCase serde default) — NOT "ANISE". Other casings, including the all-caps form, return 400.
             * @enum {string}
             */
            ephemeris: "Keplerian" | "Anise" | "Custom";
            j2?: number | null;
            j3?: number | null;
            j4?: number | null;
            third_bodies?: string[];
            keplerian_orbit?: components["schemas"]["KeplerianOrbit"] | null;
            /** @description North pole right ascension [deg], ICRF/J2000. Overrides the catalog value if set; resolved from the catalog by body name otherwise. Needed for real J2/J2J3J4 central-body fidelity when this body becomes the propagator's central body — see /api/bodies' field of the same name. */
            pole_ra_deg?: number | null;
            /** @description North pole declination [deg], ICRF/J2000. See pole_ra_deg. */
            pole_dec_deg?: number | null;
        };
        /** @description Internally tagged (serde tag = "type") union — one entry per [[spacecraft.hardware]] TOML block. Was undocumented before (generated frontend type was `{type} & Record<string, unknown>`, hiding even CustomPlate's real fields) — same class of drift as the MgaParams fix. Placement extension (spacecraft-builder design): CustomPlate, RcsThruster, and CommAntenna (new) all have real body-frame placement; StarTracker/OpNavCamera/Lidar/SolarPanel gained optional placement/boresight fields (None = unplaced, Phase-1-honest generic rendering). ReactionWheelCluster/IMU intentionally have none — see each schema's own description for why. */
        HardwareItem: components["schemas"]["HardwareItemReactionWheelCluster"] | components["schemas"]["HardwareItemRCS"] | components["schemas"]["HardwareItemRcsThruster"] | components["schemas"]["HardwareItemStarTracker"] | components["schemas"]["HardwareItemIMU"] | components["schemas"]["HardwareItemOpNavCamera"] | components["schemas"]["HardwareItemLidar"] | components["schemas"]["HardwareItemCommAntenna"] | components["schemas"]["HardwareItemSolarPanel"] | components["schemas"]["HardwareItemCustomPlate"];
        /** @description No placement field, deliberately — cluster GEOMETRY (orthogonal vs. pyramid + cant angle), not position, is what matters for a momentum-exchange device; per-wheel position/orientation is not modeled. The backend's ReactionWheelCluster is fixed at [f64;4] (4-wheel pyramid) — a real N-wheel generalization is deferred (Phase 13f note). */
        HardwareItemReactionWheelCluster: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            type: "ReactionWheelCluster";
            model?: string | null;
            count: number;
            /** @description Maximum wheel speed [rad/s]. */
            max_speed_rads?: number | null;
            /** @description Maximum output torque per wheel [N*m]. */
            max_torque_nm?: number | null;
            /** @description Wheel spin-axis inertia [kg*m^2]. */
            inertia_kgm2?: number | null;
            /** @description PER-WHEEL mass [kg] (derived-mass-properties extension). null falls back to the matching hardware_catalog::ReactionWheelSpec grade's mass_kg. No placement field on this variant — wheels are assumed to sit at the spacecraft's geometric-center origin for the mass-properties rollup. */
            mass_kg?: number | null;
        };
        HardwareItemRCS: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            type: "RCS";
            /** @description Thrust per thruster [N]. */
            thrust_n?: number | null;
            count?: number | null;
            /** @description Scalar moment arm [m] — the AGGREGATE cluster model (a canonical symmetric 12-thruster layout). Used by simulate.rs::rcs_from_hardware ONLY as a fallback when no RcsThruster entries (see that schema) are present in the same config — real placed thrusters take precedence,. */
            moment_arm_m?: number | null;
            /** @description PER-THRUSTER mass [kg]. null falls back to hardware_catalog::ThrusterSpec::monoprop.mass_kg. No placement field (aggregate cluster) — same geometric-center-origin assumption as ReactionWheelCluster.mass_kg. */
            mass_kg?: number | null;
        };
        /** @description One real, individually-placed RCS thruster. Consumed by the LIVE 6DOF simulation as of: simulate.rs::rcs_from_hardware builds the sim's actual actuator list directly from these entries, exactly as placed, whenever any are present — a genuinely asymmetric or incomplete-coverage layout is realized as-is (not smoothed into a symmetric-cluster equivalent), which is what makes an attitude-stability preview built on top of it honest. Falls back to the aggregate RCS variant only when no RcsThruster entries exist in the config. */
        HardwareItemRcsThruster: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            type: "RcsThruster";
            /** @description Thrust magnitude [N]. */
            thrust_n: number;
            /** @description Thruster position relative to spacecraft CoM, body frame [m]. */
            position_m: number[];
            /** @description Thrust direction — the direction of the FORCE applied to the spacecraft (same convention as BurnConfig::body_dir, not the nozzle/exhaust direction), body frame. Normalized on load. */
            direction: number[];
            /** @description This thruster's own mass [kg]. null falls back to hardware_catalog::ThrusterSpec::monoprop.mass_kg. */
            mass_kg?: number | null;
            /** @description This thruster's own specific impulse [s] — added. null falls back to hardware_catalog::ThrusterSpec::monoprop.isp_s, same convention as mass_kg. sim_engine::control::allocate prices RCS propellant per-thruster from this value (each thruster's own thrust_n/isp_s and the duty cycle it individually fired at), not against one aggregate scalar for the whole set — so a real layout mixing thruster classes (e.g. fine ColdGas thrusters for pointing alongside a higher-thrust Monoprop set for coarse slews) accounts for propellant honestly. */
            isp_s?: number | null;
        };
        HardwareItemStarTracker: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            type: "StarTracker";
            model?: string | null;
            /** @description 1-sigma attitude noise per axis [rad]. */
            noise_rad?: number | null;
            /** @description Boresight direction, body frame (unit; normalized on load). null = unplaced (Phase 1: rendered generically, no pointing geometry). */
            boresight?: number[] | null;
            /** @description Sensor position relative to spacecraft CoM, body frame [m]. Cosmetic only (no parallax effect modeled at spacecraft scale). */
            position_m?: number[] | null;
            /** @description Full field-of-view half-angle [deg], for the FOV-cone render and occlusion warnings. null = no cone rendered. */
            fov_deg?: number | null;
            /** @description Unit mass [kg]. null falls back to the matching hardware_catalog::StarTrackerSpec grade's mass_kg. */
            mass_kg?: number | null;
        };
        /** @description No placement field, deliberately — an IMU measures the spacecraft's own rigid-body motion, which has no dependence on where in the body it sits (to first order, ignoring lever-arm effects this model doesn't represent). */
        HardwareItemIMU: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            type: "IMU";
            /** @description 1-sigma delta-V noise per axis per step [m/s]. */
            noise_sigma_mps?: number | null;
            /** @description Unit mass [kg]. null falls back to hardware_catalog::ImuSpec::medium.mass_kg. */
            mass_kg?: number | null;
        };
        HardwareItemOpNavCamera: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            type: "OpNavCamera";
            /** @description 1-sigma bearing noise [mrad]. NOTE: a different unit from OpNavCameraSpec's bearing_noise_rad ([rad]) returned by GET /api/hardware — same physical quantity, inconsistent units between request and catalog response, known issue, not yet reconciled. */
            bearing_noise_mrad?: number | null;
            /** @description 1-sigma angular-size noise [mrad]. See bearing_noise_mrad's unit note. */
            angular_size_noise_mrad?: number | null;
            /** @description Boresight direction, body frame (unit; normalized on load). null = unplaced. */
            boresight?: number[] | null;
            /** @description Camera position relative to spacecraft CoM, body frame [m]. */
            position_m?: number[] | null;
            /** @description Full field-of-view half-angle [deg]. null = no cone rendered. */
            fov_deg?: number | null;
            /** @description Unit mass [kg]. null falls back to the matching hardware_catalog::OpNavCameraSpec grade's mass_kg. */
            mass_kg?: number | null;
        };
        HardwareItemLidar: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            type: "Lidar";
            /** @description 1-sigma range noise [m]. */
            range_noise_m?: number | null;
            /** @description Maximum operating range [m]. */
            max_range_m?: number | null;
            /** @description Boresight direction, body frame (unit; normalized on load). null = unplaced. */
            boresight?: number[] | null;
            /** @description Sensor position relative to spacecraft CoM, body frame [m]. */
            position_m?: number[] | null;
            /** @description Full field-of-view half-angle [deg]. null = no cone rendered. */
            fov_deg?: number | null;
            /** @description Unit mass [kg]. null falls back to the matching hardware_catalog::LidarSpec grade's mass_kg. */
            mass_kg?: number | null;
        };
        /** @description Comm antenna (HGA/LGA) — added (no prior representation existed). Renders identically to a star-tracker/camera boresight cone. Sets up the classic simultaneous-pointing-constraint problem (panels at Sun + HGA at Earth + camera at target) for the Phase 03 pointing-mode/attitude-commander work. */
        HardwareItemCommAntenna: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            type: "CommAntenna";
            /** @description Boresight direction, body frame (unit; normalized on load). */
            boresight: number[];
            /** @description Full beamwidth [deg] (half-angle of the comm cone — same convention as fov_deg elsewhere, named differently here since "beamwidth" is the standard RF term). */
            beamwidth_deg: number;
            /** @description Antenna position relative to spacecraft CoM, body frame [m]. Cosmetic only. */
            position_m?: number[] | null;
            /** @description Unit mass [kg]. null falls back to a representative HGA-class default (no dedicated antenna catalog exists yet). */
            mass_kg?: number | null;
        };
        /** @description The canonical placed-panel representation is "own fields" (this variant), not CustomPlate — decided because the builder mockup review wants independent live width_m/height_m resize handles and an articulation flag, neither of which CustomPlate represents. An unplaced entry (position_m/normal both null, every previous config) behaves exactly as before. */
        HardwareItemSolarPanel: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            type: "SolarPanel";
            /** @description Total panel area [m^2] — used directly for the unplaced/aggregate case (existing behavior, unchanged); ignored in favor of width_m*height_m when a placed panel supplies both. */
            area_m2: number;
            /** @description Panel electrical efficiency, 0-1. */
            efficiency?: number | null;
            /** @description Panel geometric centre relative to spacecraft CoM, body frame [m]. Placement extension — set together with normal to make this a real placed, SRP-force-contributing panel (its own Plate) instead of an unplaced entry feeding the automatic symmetric +/-y panel pair. */
            position_m?: number[] | null;
            /** @description Deployed outward normal, body frame (unit; normalized on load). See position_m. */
            normal?: number[] | null;
            /** @description Panel width [m] (in-plane). With height_m, overrides area_m2 for the plate this panel contributes. */
            width_m?: number | null;
            /** @description Panel height [m] (in-plane). See width_m. */
            height_m?: number | null;
            /** @description Articulation mechanism (refined, see docs/MP/MANUAL.md §6.5) — null = fixed (body-mounted, no articulation; panel normal is a hard spacecraft-attitude constraint). Schema-only for now, not yet consumed by any solver. */
            articulation?: components["schemas"]["PanelArticulation"] | null;
            /** @description Specular reflectivity, 0-1, for a PLACED panel's own Plate. Defaults to 0.08 when omitted. Ignored for an unplaced entry. */
            rho_s?: number | null;
            /** @description Diffuse reflectivity, 0-1, for a PLACED panel. Defaults to 0.10. See rho_s. */
            rho_d?: number | null;
            /** @description TOTAL panel mass [kg] (both wings, if this entry represents a pair). null falls back to area * hardware_catalog::PanelSpec::rigid.areal_density_kgm2 (area = width_m*height_m if both given, else area_m2). */
            mass_kg?: number | null;
        };
        /** @description Internally tagged (serde tag = "kind") union nested inside HardwareItemSolarPanel.articulation — the three real solar-array-drive mechanism classes (docs/MP/MANUAL.md §6.5). A bare-fixed panel is represented by articulation being null on the parent, not a variant of this union. */
        PanelArticulation: components["schemas"]["PanelArticulationOneAxis"] | components["schemas"]["PanelArticulationTwoAxis"];
        PanelArticulationOneAxis: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            kind: "OneAxis";
            /** @description Single rotation axis, body frame (unit; normalized on load). Exact Sun-tracking only when the Sun lies in the plane perpendicular to this axis — otherwise a real cosine loss. */
            axis: number[];
        };
        PanelArticulationTwoAxis: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            kind: "TwoAxis";
            /** @description First rotation axis, body frame (unit; normalized on load). Must not be (nearly) parallel to axis2. */
            axis1: number[];
            /** @description Second rotation axis, body frame (unit; normalized on load). A real Solar Array Drive Assembly — in the idealized unconstrained-range case, fully decouples panel pointing from spacecraft attitude. */
            axis2: number[];
        };
        /** @description The only HardwareItem variant with real body-frame placement today (Phase 13a). Adds one arbitrary flat plate to the spacecraft's FlatPlate/NPlate SRP geometry, on top of the automatic 6 bus faces and any SolarPanel entries — lets a mission describe shapes more complex than a box + symmetric panel pair (dish antenna, asymmetric deployable, instrument cover). Maps directly onto orbital_models::acceleration::srp::Plate. */
        HardwareItemCustomPlate: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            type: "CustomPlate";
            /** @description Outward unit normal in the body frame [x, y, z] (normalized on load — need not be pre-normalized). */
            normal: number[];
            /** @description Plate area [m^2]. */
            area_m2: number;
            /** @description Plate geometric centre relative to spacecraft CoM, body frame [m]. Required for SRP torque: tau_i = center_i x F_i. */
            center_offset_m: number[];
            /** @description Specular reflectivity, 0-1. Defaults to 0.30 (matching the automatic bus faces) when omitted. */
            rho_s?: number | null;
            /** @description Diffuse reflectivity, 0-1. Defaults to 0.20 (matching the automatic bus faces) when omitted. */
            rho_d?: number | null;
            /** @description Illuminated from either side (e.g. a deployable dish or panel-like plate) vs. only its outward face (e.g. a bus-mounted instrument cover). Defaults to false (single-sided) when omitted. */
            double_sided?: boolean | null;
            /** @description Plate mass [kg]. null falls back to a rough generic areal-density placeholder (CustomPlate represents an arbitrary shape with no natural structural class). */
            mass_kg?: number | null;
        };
        PropulsionConfig: {
            /** @enum {string} */
            type: "Monoprop" | "Biprop" | "Electric" | "Cold Gas";
            isp_s: number;
            thrust_n: number;
        };
        SpacecraftConfig: {
            mass_kg: number;
            dry_mass_kg: number;
            propellant_mass_kg: number;
            bus_dims_m: number[];
            inertia_diag_kgm2: number[];
            /** @description When true, the live 6DOF simulation uses the diagonal of the real parallel-axis-derived inertia tensor (bus box + every placed/itemized HardwareItem, about the true center of mass -- same computation POST /api/design/vehicle already exposes) instead of this struct's own inertia_diag_kgm2. Default/null/false: inertia_diag_kgm2 stays authoritative, byte-for-byte the previous behavior. Off-diagonal (cross-coupling) terms are not used -- the live sim's SpacecraftProperties is diagonal-only. */
            derive_inertia_from_geometry?: boolean | null;
            /** @enum {string} */
            srp_model: "Cannonball" | "FlatPlate" | "NPlate";
            cp_cg_offset_m?: number | null;
            propulsion: components["schemas"]["PropulsionConfig"];
            hardware: components["schemas"]["HardwareItem"][];
        };
        CaptureConfig: {
            /** @description Capture orbit radius at the arrival body [m]. When capture_eccentricity > 0 this is the capture orbit's PERIAPSIS radius. */
            target_orbit_radius_m?: number | null;
            approach_v_inf_mps?: number | null;
            /** @description Capture orbit eccentricity in [0, 1) (Phase 9v-ii). Default 0.0 = circular capture. When > 0 the LOI burn is applied at periapsis into an ellipse: dv = sqrt(v_inf^2 + 2*mu/r_p) - sqrt(mu*(1+e)/r_p). The GTOP Cassini-2 benchmark uses r_p = 108950 km, e = 0.98. */
            capture_eccentricity?: number;
        };
        /** @description Search-space window for the narrowing-stage solvers (GridSearch/MonteCarlo/GA/PSO) under /api/design/trajectory. All fields optional with defaults applied server-side — this schema was previously missing from the spec even though MissionPlanner/src/config.rs::TrajectoryConfig has carried it since before Phase 9. */
        CruiseConfig: {
            /** @description Minimum time-of-flight to search [days]. Defaults to 100.0 if omitted. */
            tof_days_min?: number | null;
            /** @description Maximum time-of-flight to search [days]. Defaults to 400.0 if omitted. */
            tof_days_max?: number | null;
            /** @description Number of grid points per axis for the porkchop scan. Defaults to 30 if omitted. */
            grid_resolution?: number | null;
            /** @description Width of the departure window to sweep [days], centered on trajectory.departure_epoch. Defaults to 60.0 if omitted. */
            departure_window_days?: number | null;
        };
        CruiseWindowConfig: {
            /** @description Window start [s on the reference clock], >= 0. */
            start_s: number;
            /** @description Window end [s], > start_s and <= cruise_seed.duration_s. */
            end_s: number;
            /** @description Optional position dispersion [m] added to the reference state at start_s. */
            initial_dr_m?: number[] | null;
            /** @description Optional velocity dispersion [m/s] added to the reference state at start_s. */
            initial_dv_mps?: number[] | null;
        };
        /** @description [trajectory.departure] (Phase 8e parking radius; Phase 14a departure mode). Every field optional — an empty object is ParkingOrbit with the default parking radius (body_radius + 200 km if atmospheric, 1.5 × radius if airless). */
        DepartureConfig: {
            /** @description Parking-orbit radius [m] the escape burn is sized from. Overrides both the atmospheric/airless default and, in Launch mode, parking_altitude_m. */
            parking_orbit_radius_m?: number | null;
            /**
             * @description ParkingOrbit: the spacecraft starts in the given orbit and the departure burn is onboard (single-leg search genes theta/dv/phi unchanged). Launch: the mission starts from launch_site; the launcher's upper stage delivers the injection (departure ΔV = launcher pool, capture + DSMs onboard), the search's departure genes become (v∞, RLA, DLA) and the parking orbit / injection are closed-form launch geometry (MANUAL.md §13.6). Launch requires spacecraft.launch_vehicle, launch_site, and a departure body with a catalog pole (422 otherwise).
             * @default ParkingOrbit
             * @enum {string}
             */
            mode: "ParkingOrbit" | "Launch";
            /** @description Required in Launch mode. Only the latitude enters the physics. */
            launch_site?: components["schemas"]["LaunchSiteConfig"] | null;
            /** @description Launch mode: altitude of the parking orbit reached by the ascent before the upper-stage burn [m]. Default 185,000 (~100 n.mi.). Must be > 0. */
            parking_altitude_m?: number | null;
            /** @description Launch mode: explicit parking/escape-plane inclination to the departure body's equator [deg, 0–180]. Omit for the minimum feasible plane max(|DLA|, |site latitude|). Below the site latitude = dogleg → launch_geometry.feasible_no_dogleg = false (never priced). */
            inclination_deg?: number | null;
        };
        LaunchSiteConfig: {
            /** @description Display name, e.g. "Cape Canaveral". */
            name?: string;
            /** @description Geodetic latitude [deg], positive north, in [-90, 90]. */
            lat_deg: number;
            /** @description Longitude [deg], positive east. Display / future daily-launch-window use only. */
            lon_deg?: number;
        };
        TrajectoryConfig: {
            phases: ("Cruise" | "Capture" | "Proximity" | "Survey" | "CloseOrbit" | "Flyover" | "ScienceHold" | "RadioScience" | "Landing")[];
            /**
             * @description GridSearch is Lambert's porkchop scan exposed under a separate name (identical dispatch). SA/ManifoldStitch/WSB are recognized but return an error from /api/design/trajectory today — implemented in other, unconnected codebases (OptimizationProblems / AstroProbs/LunarTrajectories).
             * @enum {string}
             */
            solver: "Hohmann" | "Lambert" | "GridSearch" | "LambertThenDiffCorrect" | "DiffCorrection" | "MonteCarlo" | "SA" | "GA" | "PSO" | "ManifoldStitch" | "WSB";
            departure_epoch?: string | null;
            /** @description Catalog body name. Now required (previously optional with a silent "Earth" fallback) — departure economics (launch-vehicle-provided vs. self-funded escape burn) and the real parking-orbit departure leg both depend on which body this is. */
            departure_body: string;
            /** @description [trajectory.departure] — parking-orbit radius and, since Phase 14a, the departure MODE (ParkingOrbit | Launch) with its launch site. Omit for the historical ParkingOrbit behaviour with the default parking radius. */
            departure?: components["schemas"]["DepartureConfig"] | null;
            capture?: components["schemas"]["CaptureConfig"] | null;
            /** @description Search-space window for GridSearch/MonteCarlo/GA/PSO. This is the field the frontend needs to expose to actually narrow the search — see CruiseConfig. */
            cruise?: components["schemas"]["CruiseConfig"] | null;
        };
        GncConfig: {
            /** @enum {string} */
            navigation_filter: "EKF" | "UKF" | "IEKF";
            /** @enum {string} */
            pointing_mode: "Nadir" | "SunPointing" | "VelocityAligned" | "Custom";
            /** @enum {string} */
            attitude_controller: "ReactionWheelPD" | "RcsBangBang";
            /** @description Defaults to 100 m if omitted */
            position_accuracy_req_m?: number | null;
            /** @description Defaults to 0.01 m/s if omitted */
            velocity_accuracy_req_mps?: number | null;
            /** @description Legacy explicit wheel-PD proportional gain override [N·m/rad]; honored when attitude_control.wheels is absent. Omit for the derived value (MANUAL.md §10.1). */
            reaction_wheel_kp?: number | null;
            /** @description Legacy explicit wheel-PD derivative gain override [N·m·s/rad]. */
            reaction_wheel_kd?: number | null;
            pointing_deadband_rad?: number | null;
            rate_deadband_radps?: number | null;
            /** @description Three-layer attitude control (MANUAL.md §10.5): per-actuator law, per-activity tuning, gain scheduling. Optional; omitted = fully derived from the vehicle. The values actually used come back in CruiseResult.attitude_control_effective. */
            attitude_control?: components["schemas"]["AttitudeControlConfig"];
        };
        /** @description gnc.attitude_control — every field optional. Layer 1: wheels (default Pd derived from the wheel cluster's max_torque) and thrusters (default Pid derived from the placed RCS layout's worst-axis torque authority — integral action removes the steady-state offset a persistent engine-misalignment torque leaves under pure PD; PhasePlane is the thruster-native alternative). Layer 2: activities. Layer 3: gain_scheduling. */
        AttitudeControlConfig: {
            wheels?: components["schemas"]["AttitudeLawConfig"];
            thrusters?: components["schemas"]["AttitudeLawConfig"];
            /** @description Per-activity tuning overrides (MANUAL.md §10.5.4). Defaults: hold 1.0/1.0, slew 1.0/1.0, burn_hold 1.0/0.5, coast 0.5/2.0 (bandwidth_scale/deadband_scale). */
            activities?: {
                hold?: components["schemas"]["ActivityTuningConfig"];
                slew?: components["schemas"]["ActivityTuningConfig"];
                burn_hold?: components["schemas"]["ActivityTuningConfig"];
                coast?: components["schemas"]["ActivityTuningConfig"];
            };
            /** @description Layer 3 (MANUAL.md §10.5.5). enabled (default true): re-derive the active law on mode/activity change or when mass moves by more than mass_change_fraction (default 0.05) of the reference mass. inertia_scales_with_mass (default false): model the scheduling inertia as I_ref·m/m_ref — off by default because the truth propagator holds inertia constant through propellant depletion; enabling it is a deliberate controller/truth mismatch until the truth model carries a propellant-inertia term. */
            gain_scheduling?: {
                enabled?: boolean | null;
                mass_change_fraction?: number | null;
                inertia_scales_with_mass?: boolean | null;
            };
        };
        /** @description One layer-1 law, tagged on `law`. Every parameter optional = derived default for that actuator class. Angles in degrees. */
        AttitudeLawConfig: {
            /** @enum {string} */
            law: "Pd" | "Pid" | "PhasePlane";
            /** @description Pd/Pid: proportional gain [N·m/rad]. */
            kp?: number | null;
            /** @description Pd/Pid: derivative gain [N·m·s/rad]. */
            kd?: number | null;
            /** @description Pid: integral gain [N·m/(rad·s)] (default k_p·ω_n/(2π·2)). */
            ki?: number | null;
            /** @description Pid: anti-windup clamp on |∫θ dt| per axis [rad·s] (default τ_authority/k_i). */
            integral_limit?: number | null;
            /** @description PhasePlane: switching-function deadband δ (default 0.5°). Must exceed τ_auth·t_min·T/I or the minimum impulse bit sets a larger limit cycle (warned). */
            deadband_deg?: number | null;
            /** @description PhasePlane: rate deadband [deg/s] (default δ/T). */
            rate_deadband_degs?: number | null;
            /** @description PhasePlane: Schmitt-trigger hysteresis (default 0.2·δ). */
            hysteresis_deg?: number | null;
            /** @description PhasePlane: minimum thruster on-time / impulse bit [s] (default 0.02). */
            min_on_time_s?: number | null;
            /** @description PhasePlane: lead time T in s = θ + T·ω [s] (default 2·k_d/k_p of the derived PD). */
            lead_time_s?: number | null;
        };
        ActivityTuningConfig: {
            /** @description Multiplies closed-loop ω_n (k_p→s²k_p, k_d→s·k_d, k_i→s³k_i, T→T/s); damping ratio invariant. */
            bandwidth_scale?: number | null;
            /** @description Multiplies phase-plane/PD dead-bands. */
            deadband_scale?: number | null;
        };
        /** @description Effective parameters of one layer-1 law as flown (CruiseResult.attitude_control_effective). source is 'derived' or 'config'. Nulls: PD/PID have no deadband fields, PhasePlane has no gains/ω_n/ζ. */
        AttitudeLawReport: {
            /** @enum {string} */
            law: "Pd" | "Pid" | "PhasePlane";
            /** @enum {string} */
            source: "derived" | "config";
            /** @description Worst-axis torque authority the law was derived against [N·m]. */
            authority_nm: number;
            kp?: number | null;
            kd?: number | null;
            ki?: number | null;
            integral_limit_rad_s?: number | null;
            deadband_deg?: number | null;
            rate_deadband_degs?: number | null;
            hysteresis_deg?: number | null;
            min_on_time_s?: number | null;
            lead_time_s?: number | null;
            /** @description Closed-loop natural frequency at the reference max inertia. */
            omega_n_radps?: number | null;
            /** @description Damping ratio (0.85 target when derived). */
            zeta?: number | null;
            /** @description ≈ 4/(ζ·ω_n); what the burn executive adds to its slew lead time. */
            settling_time_s?: number | null;
        };
        AttitudeControlEffective: {
            wheels: components["schemas"]["AttitudeLawReport"];
            thrusters: components["schemas"]["AttitudeLawReport"];
            activities: {
                /** @enum {string} */
                activity: "Hold" | "Slew" | "BurnHold" | "Coast";
                bandwidth_scale: number;
                deadband_scale: number;
            }[];
            gain_scheduling: {
                enabled: boolean;
                mass_change_fraction: number;
                inertia_scales_with_mass: boolean;
            };
            inertia_ref_kgm2: number[];
            mass_ref_kg: number;
            tick_s: number;
            /** @description e.g. a phase-plane deadband smaller than its minimum-impulse-bit limit cycle, or zero RCS authority on some axis. */
            warnings: string[];
        };
        /** @description Layer-3 schedule point — recorded on each tick the active law was (re)derived (mode/activity change or mass trigger). */
        GainSchedulePoint: {
            t_s: number;
            mass_kg: number;
            /** @enum {string} */
            control_mode: "WheelsPrimary" | "ThrustersPrimary" | "ThrustersOnly";
            /** @enum {string} */
            activity: "Hold" | "Slew" | "BurnHold" | "Coast";
            /** @enum {string} */
            law: "Pd" | "Pid" | "PhasePlane";
            kp?: number | null;
            kd?: number | null;
            ki?: number | null;
            deadband_deg?: number | null;
            omega_n_radps?: number | null;
            zeta?: number | null;
        };
        SimulationConfig: {
            /** @enum {string} */
            integrator: "RK4" | "DormandPrince45" | "Dopri5" | "Radau";
            rtol: number;
            atol: number;
            dt_truth_s: number;
            dt_meas_s: number;
            monte_carlo_runs: number;
            output_dir: string;
        };
        MissionConfig: {
            mission: {
                name: string;
                /** @enum {string} */
                objective: "Flyby" | "Orbit" | "Landing" | "Rendezvous" | "SampleReturn";
            };
            target_body: components["schemas"]["TargetBodyConfig"];
            spacecraft: components["schemas"]["SpacecraftConfig"];
            trajectory: components["schemas"]["TrajectoryConfig"];
            gnc: components["schemas"]["GncConfig"];
            simulation: components["schemas"]["SimulationConfig"];
            /** @description Phase 9 trajectory optimization stage (Layer 1b — real propagated dynamics). Null/omitted means this mission doesn't use the optimization stage; /api/optimize then rejects with 422. */
            optimization?: components["schemas"]["OptimizationConfig"] | null;
            /** @description Phase 5.2 (roadmap item 13i). When set, POST /api/simulate flies the Phase 13 translation+attitude+guidance cruise composition seeded from a real Layer-1 design/optimize result, instead of the body-centric proximity-ops path. Null/omitted (the default) preserves the existing body-centric behavior completely unchanged. */
            cruise_seed?: components["schemas"]["CruiseSeedConfig"] | null;
        };
        CruiseReferencePointConfig: {
            t_s: number;
            /** @description Heliocentric position [m] — same frame as design.rs's ArcApiPoint. */
            r_m: number[];
            /** @description Heliocentric velocity [m/s]. Not carried by ArcApiPoint today — the client is expected to have retained it from whatever real propagation produced the reference (e.g. a design/optimize result's own internal arc, before its velocity was dropped for the ArcApiPoint wire format). */
            v_mps: number[];
        };
        CruiseSeedConfig: {
            /** @description Initial heliocentric position [m], at reference[0].t_s (normally 0). */
            r0_m: number[];
            /** @description Initial heliocentric velocity [m/s]. */
            v0_m: number[];
            /** @description The Layer-1 trajectory to track — dispersion (dr_m) is always reported against this; when tcm_dr_threshold_m is set, it is also actively corrected via threshold-triggered TCM burns (item 5) targeting this same reference's own endpoint. At least 2 points, sorted ascending by t_s, with reference[0].t_s == 0 and reference[0].r_m/v_mps matching r0_m/v0_m within 1 m / 1 mm/s (check_config rejects otherwise — a mismatched start would silently report a nonzero dispersion at t=0 unrelated to real tracking quality). */
            reference: components["schemas"]["CruiseReferencePointConfig"][];
            /** @description How long to fly the leg [s]. Must not exceed the reference's own time span. */
            duration_s: number;
            /** @description Control-loop tick [s]. Must respect the vehicle/gains' closed-loop bandwidth (roughly tick <= natural_period/10..20, natural_period ~ 2*pi*sqrt(inertia/kp)) — check_config cannot verify this without duplicating that computation, so an unstable choice surfaces at runtime (divergent pointing error, integrator warnings), not as a validation error. See cruise_demo.rs's doc comment for the real incident (a 3600 s tick genuinely destabilized a real vehicle/gains combination) that established this constraint. */
            tick_s: number;
            /** @description Phase 03 — named, prioritized pointing-rule sets. Empty (the default) preserves the pre-ask-#7 behavior exactly: a fixed SunPointing attitude hold for the whole leg. See docs/MP/MANUAL.md §6.4 for why only the top 2 rules in any one mode can ever control the attitude — rule 3+ are evaluated and reported as satisfied/violated, never controlling. */
            modes?: components["schemas"]["GncModeConfig"][];
            /** @description Time-windowed assignment of modes (by name) to the leg's timeline. A tick not covered by any entry falls back to safe_mode if set, else to the legacy fixed SunPointing behavior. Overlapping entries: the LAST matching entry (list order) wins. */
            mode_schedule?: components["schemas"]["ModeScheduleEntryConfig"][];
            /** @description Name of a mode (must exist in modes) used for any tick not covered by mode_schedule — entered by schedule-gap fallback in this first cut, not real fault detection (no fault-detection system exists in this codebase yet). */
            safe_mode?: string | null;
            /** @description Precomputed heliocentric position tracks for any PointingTargetConfig Body target referenced by a rule in modes (Earth, the target body, a flyby body,...) — supplied by the client, same reasoning as reference itself (cruise.rs is deliberately ANISE-free). Since a track with a resolvable mu_m3s2 ALSO becomes a real third-body gravitational perturber, not just a pointing target — see BodyTrackConfig.mu_m3s2. POST /api/simulate also auto-appends a real track for any target_body.third_bodies name not already present here (server-side, via design::build_third_body_tracks) — this array as SENT in the request need not include those names, but the request's cruise_seed as ACTUALLY RUN will. */
            body_tracks?: components["schemas"]["BodyTrackConfig"][];
            /** @description Item 5 hardened (Phase 13m-13p): trigger a trajectory-correction maneuver the first tick position dispersion (dr_m) exceeds this [m], subject to a cooldown after the previous correction and a minimum-remaining-TOF floor. null (default) preserves the exact pre-TCM coast-only behavior. Requires spacecraft.propulsion to also be set (the thrust/Isp source for the burn) — check_config flags this combination as an error if propulsion is missing, since it would otherwise silently never fire. The correction targets a NEARER epoch than the fixed leg-end whenever that's more than a quarter of the vehicle's current local orbital period away (Phase 13p — a single-revolution Lambert solve forced to satisfy a multi-revolution time-of-flight is genuinely ill-posed, not just imprecise), re-solved continuously while reorienting (the reorientation itself can take real time, independent of main-engine thrust, during which a one-shot solve would go stale). Takes priority over pointing_mode/modes for the whole maneuver (Slewing to burn attitude, then Burning, or — for a small correction, or a pointing_locked mode — an RCS-only correction that does NOT override pointing), and is NOT guaranteed to complete within duration_s if the demanded DeltaV exceeds what spacecraft.propulsion can deliver in the remaining time — see CruiseStepMsg.tcm_phase. */
            tcm_dr_threshold_m?: number | null;
            /** @description Mode-scheduled control tick [s] used while a main-engine burn is FIRING (MANUAL.md §10.5.1): the discrete-loop margin caps attitude bandwidth at ω_n ≤ 2π/(15·tick), so a coarse cruise tick makes the thrusters' larger torque authority unusable during burns regardless of the law. The thruster-mode law is derived at THIS tick. Default when omitted: max(tick_s/10, 1 s), never longer than tick_s. Must satisfy 0 < burn_tick_s <= tick_s. */
            burn_tick_s?: number | null;
            /** @description Test window: fly only [start_s, end_s] of the leg. The truth starts at the REFERENCE state at start_s (plus the optional dispersion), r0_m/v0_m are ignored, planned burns before start_s are skipped, the tank is assumed full at start_s. For testing an approach-phase or burn change without flying the whole cruise. Null = the whole leg (unchanged behaviour). */
            window?: components["schemas"]["CruiseWindowConfig"] | null;
            /** @description Phase-adaptive reporting: stride used INSTEAD of report_stride on fast-dynamics ticks — any Slewing/Burning/RcsCorrecting tick, any tick inside a registered SOI-capture body's sphere (parking coast, capture orbit — the frontend's jagged-parking-polygon and attitude-swing-between-samples symptoms), plus every tick within a 600 s trailing window after a tcm_phase or active_mode transition (the transition tick itself always relays). Default 1 = full control-loop resolution there while cruise coast keeps the coarse budgeted stride; those windows are a small share of a mission so the total message count stays near the coast budget (the frontend's LTTB downsampling handles the rest). Relay-layer only: control-loop fidelity, max_dr_m, the on-disk CSV and mode_transitions are unaffected, exactly like report_stride. */
            report_stride_maneuver?: number | null;
            /** @description (frontend ask): decouples REPORTING cadence (the /api/simulate/:id/stream WS broadcast and the /steps REST history) from the real control-loop tick — only relay every Nth tick externally (plus always tick 0 and the final tick), while the control loop itself, max_dr_m/max_wheel_momentum_nms, the on-disk CSV, and mode_transitions all still run/compute at the full tick_s, completely unaffected. null or 1 both mean "report every tick" (the pre-existing behavior); check_config rejects 0. Needed because a long leg reported at full tick_s cadence can overflow a JSON string's max length client-side (confirmed: ERR_STRING_TOO_LONG on a real 129-day/tick_s=30 leg, ~371,500 ticks) — raising tick_s instead would also coarsen the control loop's own bandwidth, which this field deliberately does not touch. Trade-off: /cancel is only checked on reported ticks too, so worst-case cancel latency becomes report_stride * tick_s seconds instead of ~1 tick. */
            report_stride?: number | null;
            /** @description Phase 13n: scheduled burn EVENTS (a DSM on an MGA leg, an arrival/capture burn, a departure burn under GNC test) — as opposed to tcm_dr_threshold_m's REACTIVE, dispersion-triggered executive. Empty (default) preserves the exact pre-13n behavior. Fires via the SAME BurnAttitude + body-+X-fire mechanism the reactive executive uses, always through the main engine (never the RCS-only path) — takes priority over a reactive trigger on any tick both would fire. Requires spacecraft.propulsion, same as tcm_dr_threshold_m. Each entry's dv_inertial_mps fires verbatim by default; set that entry's target_epoch_s (see PlannedBurnConfig) to instead re-solve the fired ΔV fresh from the vehicle's real dispersed state at ignition — only meaningful for a DSM (a position target), not an arrival/capture or departure burn (which shape velocity, not position, and are not yet re-targetable). */
            planned_burns?: components["schemas"]["PlannedBurnConfig"][];
        };
        /** @description One scheduled burn event — see CruiseSeedConfig.planned_burns. */
        PlannedBurnConfig: {
            /** @description Mission-elapsed time to trigger this burn [s]. planned_burns must be sorted ascending by this field. */
            epoch_s: number;
            /** @description The ΔV to deliver, heliocentric/inertial frame [m/s] — direction and magnitude combined in one vector, straight from the adopted arc's own DSM/capture-burn fields. Fired verbatim unless target_epoch_s is set. */
            dv_inertial_mps: number[];
            /** @description Nuance 2: when set, the ACTUAL fired ΔV is re-solved fresh at ignition from the vehicle's real current state, targeting the reference trajectory's own recorded position at this epoch [s since departure] via the same Lambert re-solve the reactive TCM executive uses — absorbing whatever dispersion has accumulated since the last correction, instead of firing dv_inertial_mps verbatim. Falls back to dv_inertial_mps if the fresh solve fails. Must be > epoch_s and <= cruise_seed.duration_s. Only meaningful for a DSM (a position target) — leave null for an arrival/capture or departure burn, which shape velocity, not position. */
            target_epoch_s?: number | null;
            /**
             * @description : this burn is delivered by an EXTERNAL stage (a launch vehicle's upper stage for an Earth departure — the launcher-provided ΔV pool of the two-pool model, LaunchVehicleCheck), not the spacecraft's engine: applied impulsively (dv_inertial_mps verbatim) at epoch_s with no slew, no spacecraft-attitude requirement and NO draw on propellant_mass_kg. Set it on the departure burn whenever launch_vehicle_check.feasible is true for the adopted trajectory; otherwise a Phase-03 replay fires the multi-km/s injection through the onboard engine and drains the tank before cruise begins. Reported as status 'Completed (external stage)' in planned_burn_reports.
             * @default false
             */
            external_stage: boolean;
            /** @description Review D5: when set, this burn is an ARRIVAL/CAPTURE burn at the named body, and its ACTUAL fired ΔV is re-solved fresh at trigger time from the vehicle's real (dispersed) state — a VELOCITY-matching solve (scale the body-relative speed to local circular speed along the current relative-velocity direction; provably always yields a bound orbit), the genuinely different math a capture burn needs versus target_epoch_s's position-shaping Lambert re-solve. Requires a matching cruise_seed.body_tracks entry with a resolvable mu_m3s2 (explicit or catalog); mutually exclusive with target_epoch_s — both checked by check_config. Falls back to firing dv_inertial_mps verbatim if the runtime track lookup fails. Solved at trigger time only (not continuously re-solved through the slew — a velocity-matching target drifts far more slowly than a shrinking-TOF Lambert one). */
            capture_body?: string | null;
            /** @description Free-text label for telemetry/debugging only (e.g. "DSM leg 2", "Mercury capture") — never consulted by the executive itself. */
            label?: string;
        };
        /** @description Which inertial direction a PointingRuleConfig targets. Discriminated by `type`. */
        PointingTargetConfig: {
            /** @enum {string} */
            type: "Sun" | "Body" | "Velocity" | "Inertial";
            /** @description Only when type=Body — must have a matching BodyTrackConfig entry in cruise_seed.body_tracks. */
            name?: string;
            /** @description Only when type=Inertial — a fixed inertial direction. */
            direction?: number[];
        };
        /** @description One pointing rule: a placed hardware item's boresight/normal should point at target. */
        PointingRuleConfig: {
            /** @description Index into spacecraft.hardware. Must resolve to a hardware type with a real pointing direction (StarTracker/OpNavCamera/Lidar/CommAntenna/SolarPanel/CustomPlate, with boresight/normal set if optional) — check_config rejects otherwise. */
            hardware_index: number;
            target: components["schemas"]["PointingTargetConfig"];
        };
        /** @description A named, prioritized pointing-rule set (a GNC "mode"). rules' LIST ORDER is the priority order — index 0 is highest/primary, index 1 is secondary (best-effort), index 2+ are evaluated only (cannot control the attitude — see docs/MP/MANUAL.md §6.4). */
        GncModeConfig: {
            name: string;
            rules: components["schemas"]["PointingRuleConfig"][];
            /**
             * @description Marks this mode's pointing as non-negotiable (Phase 13m item #4): when active, the TCM executive prefers RCS (no slew) over the main engine for a correction rather than reorienting off this mode's rules. Defaults to false (pre-existing behavior).
             * @default false
             */
            pointing_locked: boolean;
        };
        ModeScheduleEntryConfig: {
            start_s: number;
            end_s: number;
            /** @description Must match a name in cruise_seed.modes. */
            mode: string;
        };
        /** @description A precomputed heliocentric position track for one named body -- used for PointingTargetConfig::Body pointing resolution AND (added) real third-body gravitational perturbation in the cruise loop (see mu_m3s2), and optionally (also added) real SOI-switching central-body capture (see soi_capture). */
        BodyTrackConfig: {
            name: string;
            /** @description Precomputed samples — OR omitted/empty (review B2 the SERVER-RESOLVED form): an empty track on a named ANISE-covered body asks the server to sample its own ephemeris over the leg duration before the job starts (4 samples/day, clamped to [64, 2000] points, consumed through cubic Hermite interpolation). The backend owns the kernels; shipping ephemeris client→server at client-chosen density is no longer necessary for any ANISE-covered body. A body without ANISE coverage still needs an explicit track. Since the Hermite upgrade, v_mps IS consulted (spline tangents) — supply real velocities. */
            track?: components["schemas"]["CruiseReferencePointConfig"][];
            /** @description Review B2: epoch of t_s = 0 [Julian Date] for the server-resolved form — only consulted when track is empty. Defaults to trajectory.departure_epoch when null; an empty track with neither is a 422 (nothing anchors the sampling). */
            epoch_jd?: number | null;
            /** @description Gravitational parameter [m^3/s^2] -- lets this same track ALSO drive real third-body perturbation (see CruiseStepMsg.accel_third_body_mps2), not just pointing. null falls back to a body_models::TargetBody catalog lookup by name; if that also fails (a synthetic/non-physical track, e.g. a test fixture), this track is used for pointing only, silently excluded from the perturber list. */
            mu_m3s2?: number | null;
            /**
             * @description : registers this track as a real SOI-switching CENTRAL-BODY candidate in the cruise loop's propagated truth, not just a third-body perturber. false (default) preserves every prior behavior: soi_radius_m is always null for every body_track, so the propagator's own central-body resolution (crates/sim_engine::propagator6dof, reusing trajectory_solver::resolve_central_body) can never select it -- the cruise loop's truth state stays under Sun-only central gravity even through a real close approach. This was the confirmed root cause of a genuine captured orbit (e.g. around Mercury) reading as a flyby in a mission-validation replay: the reference trajectory (built from a real Phase-01 result) correctly showed the captured orbit, but the propagated TRUTH never switched central body, so dispersion against that reference blew up as if the spacecraft flew straight through. true sizes a real Laplace SOI radius (heliocentric distance at the track's first sample when the body's catalog primary is null, e.g. Mercury; distance from the primary's OWN body_track when primary is set and that track is also supplied, e.g. the Moon needs distance from Earth -- falls back to heliocentric distance with a server-side warning if the primary's track isn't present) and a real radius_m for the collision/close-approach check. check_config rejects soi_capture: true on a track whose body resolves no mu_m3s2 (neither an explicit override nor a body_models catalog match) -- setting the flag with no way to size an SOI would otherwise silently stay third-body-only, defeating the point without saying why.
             * @default false
             */
            soi_capture: boolean;
        };
        /** @description One streamed cruise-loop tick, sent over /stream when the job's cruise_seed was set. A sibling of SimStepMsg, not a reuse — no navigation-filter fields (Phase 13h/navigation isn't built yet; this streams TRUTH state only), dispersion-against-reference and GNC-telemetry fields in their place (the design notes backlog item #6). msg_type (added, dispatch-hardening ask): distinguishes this from CruiseMcRunMsg at parse time -- a real client bug sent cruise_seed + monte_carlo_runs: 1, which server-side dispatches to the CruiseMc job kind instead, and the client blindly parsed the resulting CruiseMcRunMsg frames as this type and crashed reading r_m off an object without it. Which shape a job's stream carries was always decided by the request you made (cruise_seed alone vs. cruise_seed + monte_carlo_runs > 0); msg_type lets a consumer verify that assumption at parse time instead of trusting it got the request right. */
        CruiseStepMsg: {
            /**
             * @description Always "cruise_tick" -- the discriminant that distinguishes this frame from CruiseMcRunMsg.
             * @enum {string}
             */
            msg_type: "cruise_tick";
            t_s: number;
            r_m: number[];
            v_mps: number[];
            /** @description Attitude quaternion [w, x, y, z] — same layout convention as SimStepMsg.q. */
            q: number[];
            /** @description The commanded/target attitude quaternion [w, x, y, z] this tick — what pointing_mode (or the active GncCommander rule solve) decided the spacecraft should be pointed at. q is what it actually achieved; the quaternion-PD control law drives the two together. */
            q_cmd: number[];
            wheel_speeds_radps: number[];
            wheel_momentum_nms: number;
            /** @description Angle between current and commanded attitude (SunPointing, or the active GncCommander mode's solved attitude when cruise_seed.modes is non-empty). */
            pointing_error_deg: number;
            /** @description Body-frame torque [N*m] the quaternion-PD law computed from (q, q_cmd, omega) BEFORE wheel-speed-saturation zeroing — what the controller wanted to apply. */
            torque_cmd_body_nm: number[];
            /** @description Body-frame torque [N*m] actually applied this tick, post-saturation — what the controller could deliver given any wheel already pinned at its speed limit. Differs from torque_cmd_body_nm exactly when a wheel is saturated. */
            torque_delivered_body_nm: number[];
            /** @description |actual_r - reference_r| [m] at this tick — always reported; also actively corrected via TCM burns when cruise_seed.tcm_dr_threshold_m is set (item 5). */
            dr_m: number;
            dv_mps: number;
            rcs_propellant_kg_cum: number;
            /** @description 0-1, fraction of max wheel speed (most-saturated wheel) — same formula as SimStepMsg.wheel_sat_frac. */
            wheel_sat_frac: number;
            /** @description Review E3 ( wire name pinned by the frontend's Fig. 5): the UNCLAMPED per-wheel motor-torque command [N·m] this tick's attitude demand implied (same wheel ordering as wheel_speeds_radps). Pair with wheel_torque_delivered_nm: when any wheel clamps, the delivered body torque differs from the commanded one in DIRECTION, which the speed-only wheel_sat_frac never shows. WheelsPrimary: the pure attitude command; ThrustersPrimary: the proportional unload command; ThrustersOnly: zeros. */
            wheel_torque_cmd_nm?: number[];
            /** @description Review E3: the DELIVERED per-wheel motor torque [N·m] — post ±max_torque clamp and speed-limit zeroing. */
            wheel_torque_delivered_nm?: number[];
            /** @description Body angular velocity [rad/s], body frame (ask bundled with E3) — same shape as the slew-test messages' omega_radps; the standard missing piece for diagnosing slew behavior. */
            omega_radps?: number[];
            /** @description Which cruise_seed.planned_burns entry the current Slewing/Burning phase belongs to (burn-executive ask). null while coasting, RCS-correcting, or in a REACTIVE (dispersion-triggered) maneuver — so a reactive burn firing near a planned epoch is distinguishable from the planned burn itself. */
            planned_burn_idx?: number | null;
            /** @description Set on exactly the tick a planned burn is declared missed ("MissedBurnTimeout": attitude not settled within the go/no-go window after its epoch — burn skipped, executive back to Coast). null otherwise. */
            planned_burn_fault?: string | null;
            /** @description Review E3: max_i |wheel_motor_torque_cmd_nm[i]| / max_torque — the torque-authority twin of the speed-based wheel_sat_frac. Deliberately uncapped: EXCEEDS 1.0 when the demand overloads the wheels (the overload ratio is the useful signal). */
            wheel_torque_sat_frac?: number;
            /** @description spacecraft.propellant_mass_kg minus rcs_propellant_kg_cum minus tcm_propellant_kg_cum — RCS desaturation pulses and (since) TCM burns are the two propellant sinks modeled; a translational burn beyond TCM correction is still not modeled. */
            propellant_remaining_kg: number;
            /** @description Disturbance torque magnitude [N*m] from gravity-gradient, at this tick's environment (propagator6dof::disturbance_torque_breakdown, Phase 13b). */
            torque_gravity_gradient_nm: number;
            /** @description Disturbance torque magnitude [N*m] from SRP (flat-plate only; 0 for a Cannonball-SRP vehicle, since cannonball SRP produces no torque by construction). */
            torque_srp_nm: number;
            /** @description Translational acceleration magnitude [m/s^2] from central-body gravity (point-mass + optional zonal), at this tick's environment (propagator6dof::translational_accel_breakdown). */
            accel_central_gravity_mps2: number;
            /** @description Real when cruise_seed.body_tracks resolves any perturber (a catalog-matched body name, or one with an explicit mu_m3s2) -- was structurally always 0.0 before since the cruise loop registered no third-body perturbers at all until then. */
            accel_third_body_mps2: number;
            /** @description Always 0.0 in this first cut — SRP is not wired into translation for the decoupled step_tick path this loop uses (a documented, real Layer 1/Layer 2 fidelity gap, not an omission specific to this field). */
            accel_srp_mps2: number;
            /** @description Phase 03 — which named GncModeConfig was active this tick. null when no GncCommander was supplied (legacy fixed-pointing-mode run) or the commander had no mode to select (no schedule/safe_mode coverage). */
            active_mode?: string | null;
            /** @description Worst (largest) angular error among the active mode's rules beyond the top 2 (which cannot control the attitude — see docs/MP/MANUAL.md §6.4). null when no commander is active or the active mode has 2 or fewer rules. */
            max_rule_violation_deg?: number | null;
            /** @description Which rule achieved max_rule_violation_deg. */
            worst_violated_rule_label?: string | null;
            /**
             * @description TCM executive state this tick (item 5): null while coasting/TCM disabled, "Slewing" while reorienting to burn attitude (takes priority over active_mode/pointing_mode), "Burning" while the finite burn is actually firing, "RcsCorrecting" while an RCS-only correction fires with no slew (item #4 pointing stays under normal control).
             * @enum {string|null}
             */
            tcm_phase: null | "Slewing" | "Burning" | "RcsCorrecting";
            /** @description Cumulative main-engine propellant consumed by TCM burns [kg] — separate from rcs_propellant_kg_cum (a different propellant system, spacecraft.propulsion vs. RCS thrusters). Always 0.0 when TCM wasn't configured/never triggered. */
            tcm_propellant_kg_cum: number;
            /** @description Cumulative REAL delivered TCM/burn translational ΔV [m/s] (frontend ask 2d) — Tsiolkovsky on the mass each burning tick actually lost, plus the projected RCS impulse per RcsCorrecting tick. Accumulated after the tick's row is built, so a tick's value covers everything through the PREVIOUS tick (same convention as the burning-tick half of tcm_propellant_kg_cum). */
            tcm_dv_mps_cum?: number;
            /** @description The reactive-executive correction SOLVE that ran this tick, if any (frontend ask 2d) — a tick carrying one is always relayed regardless of report_stride, so DECLINED solves (which leave the phase in Coast and would otherwise be dropped by a coarse cruise stride) are visible on the stream too. null on the vast majority of ticks. */
            tcm_solve?: components["schemas"]["TcmSolveInfo"] | null;
            /**
             * @description Three-layer attitude control (MANUAL.md §10.5): the allocation mode actually used this tick (ThrustersPrimary during a main-engine burn).
             * @enum {string}
             */
            control_mode?: "WheelsPrimary" | "ThrustersPrimary" | "ThrustersOnly";
            /**
             * @description Layer-1 law that produced torque_cmd_body_nm this tick.
             * @enum {string}
             */
            controller_law?: "Pd" | "Pid" | "PhasePlane";
            /**
             * @description Layer-2 activity this tick (§10.5.4).
             * @enum {string}
             */
            control_activity?: "Hold" | "Slew" | "BurnHold" | "Coast";
            /** @description Effective k_p in force this tick (after activity tuning + scheduling); null for PhasePlane. */
            controller_kp?: number | null;
            controller_kd?: number | null;
            /** @description Layer 3: set on exactly the ticks the active law was (re)derived; such ticks are always relayed regardless of report_stride. */
            gain_schedule_point?: components["schemas"]["GainSchedulePoint"] | null;
        };
        /** @description One mode transition detected in a completed cruise run (Phase 03 'mode transitions are real slews with reported time/propellant/wheel cost'). settling_time_s uses the same threshold-crossing convention as /api/design/slew-test. */
        ModeTransitionReport: {
            t_s: number;
            /** @description null for the very first mode entered (transition from 'no mode'). */
            from_mode: string | null;
            to_mode: string;
            /** @description Time since the transition until pointing error first dropped to/below the settle threshold (0.5 deg) — null if it never did before the next transition or the run's end. */
            settling_time_s: number | null;
            rcs_propellant_kg_used: number;
            max_wheel_momentum_nms: number;
        };
        /** @description Final result for a cruise-seeded /api/simulate job — a sibling of SimResult (no phase list, this loop flies one continuous leg; final/peak dispersion and wheel-momentum fields in its place). */
        CruiseResult: {
            final_r_m: number[];
            final_v_mps: number[];
            final_dr_m: number;
            final_dv_mps: number;
            /** @description Peak position dispersion over the whole run. */
            max_dr_m: number;
            max_wheel_momentum_nms: number;
            rcs_propellant_kg_used: number;
            /** @description Cumulative main-engine propellant consumed by TCM burns [kg] (item 5) — 0.0 when TCM wasn't configured/never triggered. */
            tcm_propellant_kg_used: number;
            final_propellant_remaining_kg: number;
            /** @description Local filesystem path, not an HTTP-fetchable URL — file serving isn't implemented yet. */
            cruise_csv_path: string;
            /** @description Empty when no GncCommander was active (legacy fixed-pointing-mode run). */
            mode_transitions: components["schemas"]["ModeTransitionReport"][];
            /** @description Review C2: empirical interpolation floor of the submitted reference [m] — leave-one-out cross-validation of the same cubic Hermite interpolation the cruise loop uses (reconstruct each interior sample from its two neighbors, max position error, divided by 16 for the O(Δt⁴) scaling from the 2Δt leave-one-out span to the production Δt span). An order-of-magnitude warning threshold, not a precise bound. null when the reference has fewer than 3 samples. */
            reference_interpolation_floor_m?: number | null;
            /** @description Review C2: human-readable configuration warnings that are not hard check_config errors. Currently: tcm_dr_threshold_m set below reference_interpolation_floor_m (TCM would partly chase interpolation noise rather than real dispersion — raise the threshold or supply a denser reference). Empty when nothing is worth flagging. */
            warnings?: string[];
            /** @description One entry per cruise_seed.planned_burns item, in config order (burn-executive ask: real slew-start/ignition/completion epochs vs. the configured epoch, and missed-burn faults as reported findings). Empty when no planned burns were configured. */
            planned_burn_reports?: components["schemas"]["PlannedBurnReport"][];
            /** @description Frontend ask 2d: one entry per REACTIVE (dispersion-triggered) maneuver, in time order — trigger/ignition/completion epochs, the governing solve's targeting/iterations/convergence/final miss, executed ΔV and propellant — plus zero-ΔV entries for DECLINED trigger solves, so the executive's 'looked, chose not to fire' decisions are on the record. The reactive twin of planned_burn_reports; render next to those rows. Empty when reactive TCM never triggered. */
            reactive_burn_reports?: components["schemas"]["ReactiveBurnReport"][];
            /** @description Three-layer attitude control (MANUAL.md §10.5): every derived/overridden law parameter the run actually used — a run is reproducible from this block. */
            attitude_control_effective?: components["schemas"]["AttitudeControlEffective"];
            /** @description Layer 3: one entry per tick the active law was (re)derived, in time order. The same points are streamed on their ticks (CruiseStepMsg.gain_schedule_point), which are always relayed regardless of report_stride. */
            gain_schedule_points?: components["schemas"]["GainSchedulePoint"][];
        };
        /** @description Execution report for one planned burn. The executive now starts the slew BEFORE the configured epoch by a computed lead time (kinematic slew time at the profiled cruise rate + a settle margin), HOLDS the inertially-fixed burn attitude, and ignites at the epoch (never before; late only if the attitude had not settled by then, in which case the ΔV is re-solved at actual ignition). If the attitude has not settled within a go/no-go window after the epoch, the burn is declared missed and skipped. All epochs are on the cruise_seed clock (the same t_s the tick stream uses) — compare directly against configured_epoch_s to diagnose early/late ignition without guessing which clock the executive triggered on. */
        PlannedBurnReport: {
            index: number;
            label: string;
            configured_epoch_s: number;
            /** @description First Slewing tick for this burn — before configured_epoch_s when the lead-time scheduling caught it in time. */
            slew_start_s?: number | null;
            /** @description First Burning tick (real ignition). For a non-capture burn, >= configured_epoch_s by construction. For a capture_body burn, ignition is gated on the DISPERSED trajectory's own periapsis passage (hyperbolic Kepler, recomputed per tick), so it may precede or follow the stored epoch — firing a velocity-matching burn at the nominal clock time instead captured into whatever orbit the ship's current radius implied (measured: ignition at 13,998 km vs a 6,681 km planned periapsis bought a barely-bound a=261,617 km orbit). */
            ignition_s?: number | null;
            /** @description First Coast tick after the burn delivered its full ΔV. null for a missed burn. */
            completed_s?: number | null;
            /**
             * @description NotReached: the leg ended before the burn's lead window opened. BurnAttitudeAbort: the burn was cut off after pointing >20° off for 30 s of burn time.
             * @enum {string}
             */
            status: "Completed" | "MissedBurnTimeout" | "BurnAttitudeAbort" | "NotReached";
            /** @description Capture burns only (achieved-vs-planned ask): the orbit the burn ACTUALLY bought about its capture_body, from the truth's state at completion — compare against the Phase 01 result's post_capture_orbit_* fields. Null for non-capture burns, faulted/unreached burns, or an unresolvable body track. */
            achieved_capture?: components["schemas"]["AchievedCaptureReport"] | null;
        };
        AchievedCaptureReport: {
            /** @description Specific orbital energy about the capture body is negative (a real capture). */
            bound: boolean;
            /** @description Body-relative radius [m] at ignition — the single number that decides what orbit a velocity-matching burn can buy. */
            ignition_radius_m?: number | null;
            /** @description Semi-major axis [m]; null when not bound. */
            sma_m?: number | null;
            eccentricity: number;
            periapsis_m?: number | null;
            apoapsis_m?: number | null;
            period_s?: number | null;
        };
        /** @description One reactive-executive correction SOLVE, reported on the tick it ran (frontend ask 2d) — previously 'solver converged' vs 'returned the guess' was only distinguishable via TCM_DEBUG on the server console. Carried on CruiseStepMsg.tcm_solve; the per-tick Slewing re-target solves are deliberately NOT reported (one per tick — noise, and never what actually fires): the trigger-time and ignition-time solves are. */
        TcmSolveInfo: {
            /**
             * @description Trigger: the Coast-phase dispersion trigger's solve. Ignition: the settled-attitude re-solve that decides what actually burns (the governing one for a main-engine correction). RcsTrigger: the RCS path's refine, which has no ignition step.
             * @enum {string}
             */
            context: "Trigger" | "Ignition" | "RcsTrigger";
            /**
             * @description BPlane/Position: which shooting formulation refined the correction under the real force model (B-plane targeting at the approach body when the horizon is a reference SOI entry; fixed-time-of-arrival position targeting otherwise). Lambert: only the two-body guess was solved (the direct trigger path — refinement happens at ignition there).
             * @enum {string}
             */
            targeting: "BPlane" | "Position" | "Lambert";
            /** @description Epoch [s, cruise clock] the solve aimed at — the SOI entry for a B-plane solve, the bounded Lambert horizon otherwise. */
            aim_epoch_s: number;
            /** @description |ΔV| [m/s] of the two-body Lambert guess the solve started from. */
            lambert_dv_mps?: number | null;
            /** @description |ΔV| [m/s] of the solve's own result — what the decision below was made on. */
            solved_dv_mps: number;
            /** @description Newton iterations the shooting refinement actually performed — 0 with a shooting targeting means it returned the guess untouched. null when no refinement ran (targeting == Lambert). */
            iterations?: number | null;
            /** @description Whether the shooting refinement met its miss tolerance. null when no refinement ran. */
            converged?: boolean | null;
            /** @description Final residual miss [m] at the aim point under the real force model. */
            miss_m?: number | null;
            /** @description Predicted delivery miss [m] with NO burn (B-plane path only) — the (2b) trigger metric; the approach trigger declines to correct when this is already within tcm_dr_threshold_m. */
            no_burn_miss_m?: number | null;
            /**
             * @description What the executive did with the solve. ReSlew: the refined direction moved past the re-slew gate, so the vehicle re-slews instead of firing along the stale attitude. DeclinedDeliveryFine: no-burn miss already within threshold. DeclinedNotWorth: non-converged and not clearly halving the dispersion — a finding, not a maneuver.
             * @enum {string}
             */
            decision: "FireMainEngine" | "FireRcs" | "Ignite" | "ReSlew" | "DeclinedDeliveryFine" | "DeclinedNotWorth" | "AbortedAtIgnition";
        };
        /** @description Per-reactive-maneuver execution report (frontend ask 2d) — the reactive twin of PlannedBurnReport, on CruiseResult.reactive_burn_reports. All epochs on the cruise_seed clock. The solve fields come from the maneuver's GOVERNING solve (the last ignition-time / RCS-trigger solve), except lambert_dv_mps which is always the trigger-time two-body guess. */
        ReactiveBurnReport: {
            /** @description First tick of the maneuver (Slewing/RcsCorrecting entry), or the declined solve's own tick. */
            trigger_epoch_s: number;
            /**
             * @description None: a declined solve — nothing fired.
             * @enum {string}
             */
            actuator: "MainEngine" | "Rcs" | "None";
            /** @description The governing solve's targeting — see TcmSolveInfo.targeting. */
            targeting?: string | null;
            aim_epoch_s?: number | null;
            /** @description |ΔV| [m/s] of the trigger-time two-body Lambert guess. */
            lambert_dv_mps?: number | null;
            /** @description |ΔV| [m/s] the governing solve produced. */
            solved_dv_mps?: number | null;
            shooting_iterations?: number | null;
            shooting_converged?: boolean | null;
            /** @description The governing solve's final residual miss [m] at its aim point. */
            predicted_miss_m?: number | null;
            no_burn_miss_m?: number | null;
            /** @description First Burning/RcsCorrecting tick — null when nothing fired. */
            ignition_s?: number | null;
            /** @description First tick back in Coast — null when the leg ended mid-maneuver. */
            completed_s?: number | null;
            /** @description Real delivered translational ΔV [m/s] over the maneuver — Tsiolkovsky per burning tick / projected RCS impulse per correcting tick, NOT the commanded impulse. */
            executed_dv_mps: number;
            /** @description Propellant this maneuver consumed [kg] (main-engine + RCS-TCM pool). */
            propellant_kg: number;
            /**
             * @description AbortedAtIgnition: the slew ran but the ignition re-solve refused to fire (non-converged / not worth it). Unfinished: the leg ended mid-maneuver (executed_dv/propellant then undercount the final tick).
             * @enum {string}
             */
            status: "Completed" | "Declined" | "AbortedAtIgnition" | "Unfinished";
        };
        CruiseStepsResponse: {
            steps: components["schemas"]["CruiseStepMsg"][];
        };
        /** @description One completed Monte Carlo run's result, sent over /stream when the job was started with cruise_seed set AND simulation.monte_carlo_runs > 0 (the design notes backlog item #10) -- one message per completed run, never per-tick, mirroring McRunMsg's convention. Disperses spacecraft.mass_kg, its inertia diagonal, (Cannonball SRP only) reflectivity, AND (added) real HardwareItem PLACEMENT geometry -- RcsThruster position/thrust-vector alignment and CustomPlate/placed-SolarPanel position/normal alignment (5mm/0.5deg and 1cm/1deg 1-sigma respectively) -- see cruise.rs's disperse_hardware_placement doc comment for the exact scope (sensor/antenna boresight dispersion is still NOT included). Placement dispersion isn't reported as its own field here (unlike mass_kg_used/reflectivity_used/inertia_diag_kgm2_used) since a vehicle can have an arbitrary number of dispersed hardware items -- its effect shows up through the same tracked outputs (max_dr_m, final_pointing_error_deg, max_wheel_momentum_nms, propellant_remaining_kg). IMPORTANT: in the current physics model (no burns, SRP not wired into translation), mass/reflectivity dispersion has NO effect on any tracked output (gravity is mass-independent; Cannonball SRP produces zero torque by definition) -- inertia and RCS/plate-geometry dispersion are the axes with a real, currently-observable effect (RCS placement/alignment on propellant efficiency and control authority; plate geometry on SRP torque under FlatPlate only). msg_type (added) distinguishes this from CruiseStepMsg at parse time -- see that schema's own description for the real bug this prevents. */
        CruiseMcRunMsg: {
            /**
             * @description Always "cruise_mc_run" -- the discriminant that distinguishes this frame from CruiseStepMsg.
             * @enum {string}
             */
            msg_type: "cruise_mc_run";
            run_index: number;
            seed: number;
            mass_kg_used: number;
            /** @description null when the vehicle's SRP model isn't Cannonball. */
            reflectivity_used: number | null;
            inertia_diag_kgm2_used: number[];
            final_dr_m: number;
            max_dr_m: number;
            final_pointing_error_deg: number;
            max_wheel_momentum_nms: number;
            propellant_remaining_kg: number;
        };
        /** @description Final summary once all cruise Monte Carlo runs complete -- analogous to McSummaryResult for the body-centric MC path. */
        CruiseMcSummaryResult: {
            n_runs: number;
            mean_max_dr_m: number;
            min_max_dr_m: number;
            max_max_dr_m: number;
            /** @description Local filesystem path, not an HTTP-fetchable URL. */
            mc_summary_csv_path: string;
        };
        CruiseMcRunsResponse: {
            runs: components["schemas"]["CruiseMcRunMsg"][];
        };
        ValidationSummary: {
            name?: string;
            objective?: string;
            body?: {
                [key: string]: unknown;
            };
            spacecraft?: {
                [key: string]: unknown;
            };
            trajectory?: {
                [key: string]: unknown;
            };
            gnc?: {
                [key: string]: unknown;
            };
            simulation?: {
                [key: string]: unknown;
            };
        };
        ValidationResponse: {
            valid: boolean;
            errors: string[];
            summary: components["schemas"]["ValidationSummary"] | null;
        };
        /** @description One sampled point along a propagated transfer arc, for 3D visualization. Units: metres, SI. */
        ArcApiPoint: {
            /** @description Time since departure [s] */
            t_s: number;
            x_m: number;
            y_m: number;
            z_m: number;
            /** @description Which body was gravitationally central at this point (Phase 7k SOI-switching). "Sun" when the spacecraft was outside every candidate body's SOI (the reference frame), else the name of the body whose SOI it was inside. */
            central_body: string;
            /** @description For MGA results: 0-indexed leg number this point belongs to (leg 0 = departure body to first flyby, leg 1 = first flyby to second, etc.). null for single-leg GA/PSO results. */
            leg_idx?: number | null;
            /** @description Real velocity x-component [m/s], same frame as x_m/y_m/z_m. Always present as of: GA/PSO transfer arcs, every pre_departure_orbit_arc/post_capture_orbit_arc (both search methods), AND MGA's own multi-leg arc (its sampling now keeps the velocity it always computed). Still typed nullable for results saved before the respective field landed. */
            vx_mps?: number | null;
            /** @description Real velocity y-component [m/s]. See vx_mps. */
            vy_mps?: number | null;
            /** @description Real velocity z-component [m/s]. See vx_mps. */
            vz_mps?: number | null;
        };
        HohmannApiResult: {
            label: string;
            r1_km: number;
            r2_km: number;
            tof_s: number;
            dv_dep_ms?: number;
            dv_arr_ms?: number;
            dv_total_ms: number;
            c3_km2s2?: number;
            /** @description Added. Only present for the "cruise_heliocentric" entry (the departure leg) on an Earth departure with a real configured vehicle — always null for "body_centric_loi" (an arrival/capture burn, not a departure). */
            launch_vehicle_check?: components["schemas"]["LaunchVehicleCheckApiResult"] | null;
            /** @description Sampled transfer-ellipse path (60 points). NOTE: orientation is synthetic — departure is placed along an arbitrary +x axis since Hohmann's analytical model has no real inertial-frame context. Shape/timing are physically correct; absolute direction is not tied to real heliocentric/body-fixed axes. Contrast with BestArcApiResult.arc, which uses real ephemeris-derived positions. */
            arc: components["schemas"]["ArcApiPoint"][];
        };
        BestArcApiResult: {
            dep_offset_days?: number;
            dep_jd?: number;
            tof_days?: number;
            dv_dep_ms?: number;
            /** @description Objective-aware arrival ΔV [m/s] (frontend backlog #14/#15): 0.0 for a Flyby mission — Flyby never burns to arrive, mirroring OptimizeApiResult.dv_arrival_ms's convention on the Phase 9 real-dynamics path. For Orbit/Landing, a real vis-viva capture burn (√(v∞²+2μ/r_p) − √(μ(1+e)/r_p), r_p floor-clamped to the target body's physical radius — the same formula mga_scan_run.rs::capture_dv_ms and mga.rs::arrival_dv_ms use) when [trajectory.capture].target_orbit_radius_m is configured, else the raw arrival v∞ as a fallback. Rendezvous/SampleReturn are unchanged: the raw arrival v∞ (Rendezvous's arrival burn is deliberately a full relative-velocity match, not a periapsis-radius capture). */
            dv_arr_ms?: number;
            /** @description dv_dep_ms + dv_arr_ms, using the objective-aware dv_arr_ms above. */
            dv_total_ms?: number;
            c3_km2s2?: number;
            /** @description Raw hyperbolic excess speed at arrival [m/s] — a physical quantity, never objective-adjusted (unlike dv_arr_ms). */
            v_inf_arr_ms?: number;
            /** @description Onboard propellant pool only (Tsiolkovsky) — never includes launcher-provided ΔV. See launch_vehicle_check for the separate launcher pool. */
            dv_budget_ms?: number | null;
            /** @description dv_budget_ms minus the onboard ΔV this trajectory actually requires — the PRICED arrival burn (dv_arr_ms, not the raw v∞: a sign error fixed) plus whatever part of the parking-orbit escape burn the launcher does not cover. Same number as dv_ledger.budget_margin_ms; kept for backward compatibility. */
            budget_margin_ms?: number | null;
            /** @description Two-pool ΔV ledger (Phase 14e). Null only when the departure body is not in the body catalog. */
            dv_ledger?: components["schemas"]["DvLedgerApiResult"] | null;
            /** @description Added. Non-null only for an Earth departure with a real, catalog-recognized [spacecraft].launch_vehicle configured. */
            launch_vehicle_check?: components["schemas"]["LaunchVehicleCheckApiResult"] | null;
            /** @description Sampled transfer-arc path (60 points), real heliocentric departure/arrival positions — not synthetic, unlike HohmannApiResult.arc. Empty if the Lambert re-solve at the best point unexpectedly failed. */
            arc: components["schemas"]["ArcApiPoint"][];
        };
        PorkchopApiPoint: {
            dep_offset_days?: number;
            tof_days?: number;
            /** @description dv_dep_ms + dv_arr_ms, using the objective-aware dv_arr_ms below. */
            dv_total_ms?: number;
            dv_dep_ms?: number;
            /** @description Objective-aware arrival ΔV [m/s] (frontend backlog #14/#15) — same convention as BestArcApiResult.dv_arr_ms: 0.0 for Flyby, a real vis-viva capture burn for Orbit/Landing (falling back to the raw arrival v∞ if [trajectory.capture].target_orbit_radius_m isn't configured), raw arrival v∞ unchanged for Rendezvous/SampleReturn. */
            dv_arr_ms?: number;
            c3_km2s2?: number;
            /** @description Raw hyperbolic excess speed at arrival [m/s] — a physical quantity, never objective-adjusted (unlike dv_arr_ms). */
            v_inf_arr_ms?: number;
        };
        /** @description One Monte Carlo sample. Only the stride-selected subset (~30 of the full scatter) carries a `trajectory`; the rest have `trajectory: null` and scalar fields only. */
        MonteCarloSampleApiResult: {
            dep_offset_days: number;
            tof_days: number;
            valid: boolean;
            dv_total_ms?: number | null;
            c3_km2s2?: number | null;
            v_inf_arr_ms?: number | null;
            /** @description Sparse propagated trajectory (~18-19 points), only for the stride-selected subset. Monte Carlo never gets the dense per-sample stream — see the design notes. */
            trajectory?: components["schemas"]["ArcApiPoint"][] | null;
        };
        /** @description Local Gaussian-scatter robustness probe around the porkchop best point (Phase 2 scope, not a full state/hardware Monte Carlo). Populated only when `solver = "MonteCarlo"`. */
        MonteCarloApiResult: {
            n_samples: number;
            n_valid: number;
            sigma_dep_offset_days: number;
            sigma_tof_days: number;
            mean_dv_total_ms: number;
            std_dv_total_ms: number;
            min_dv_total_ms: number;
            max_dv_total_ms: number;
            /** @description Porkchop-best ΔV total this scatter is centered on. */
            reference_dv_total_ms: number;
            samples: components["schemas"]["MonteCarloSampleApiResult"][];
        };
        /** @description Best individual/particle from a GA or PSO search over (departure offset, TOF). Populated only when `solver = "GA"` or `"PSO"`. */
        OptimizerApiResult: {
            /** @enum {string} */
            solver: "GA" | "PSO";
            dep_offset_days: number;
            dep_jd: number;
            tof_days: number;
            /** @description Value minimized: C3 [km²/s²] for Flyby, ΔV total [m/s] otherwise. */
            fitness: number;
            /** @enum {string} */
            fitness_units: "km2/s2" | "m/s";
            c3_km2s2: number;
            v_inf_arr_ms: number;
            dv_total_ms: number;
            /** @description Onboard propellant pool only (Tsiolkovsky) — never includes launcher-provided ΔV. See launch_vehicle_check for the separate launcher pool. */
            dv_budget_ms?: number | null;
            /** @description Same number as dv_ledger.budget_margin_ms (kept for backward compatibility). */
            budget_margin_ms?: number | null;
            /** @description Two-pool ΔV ledger (Phase 14e). Null only when the departure body is not in the body catalog. */
            dv_ledger?: components["schemas"]["DvLedgerApiResult"] | null;
            /** @description Added. Non-null only for an Earth departure with a real, catalog-recognized [spacecraft].launch_vehicle configured. */
            launch_vehicle_check?: components["schemas"]["LaunchVehicleCheckApiResult"] | null;
            /** @description Best-fitness-so-far per generation (GA) or iteration (PSO). */
            convergence: number[];
            arc: components["schemas"]["ArcApiPoint"][];
        };
        TrajectoryApiResult: {
            solver: string;
            warnings: string[];
            hohmann: components["schemas"]["HohmannApiResult"][];
            best_arc?: components["schemas"]["BestArcApiResult"] | null;
            porkchop: components["schemas"]["PorkchopApiPoint"][];
            /** @description Populated only for solver = "MonteCarlo". */
            monte_carlo?: components["schemas"]["MonteCarloApiResult"] | null;
            /** @description Populated only for solver = "GA" or "PSO". */
            optimizer?: components["schemas"]["OptimizerApiResult"] | null;
        };
        GncDesign: {
            orbit_period_s: number;
            gravity_gradient_torque_nm: number;
            srp_torque_nm: number;
            total_disturbance_torque_nm: number;
            selected_wheel: string;
            wheel_count: number;
            peak_momentum_nms: number;
            wheel_torque_margin_ok: boolean;
            selected_thruster: string;
            propellant_per_orbit_kg: number;
            selected_opnav: string;
            achieved_position_accuracy_m: number;
            achieved_velocity_accuracy_mps: number;
            ekf_state_dim: number;
        };
        VehicleRequest: {
            config: components["schemas"]["MissionConfig"];
            /** @description Spacecraft->Sun unit direction, ALREADY IN THE BODY FRAME (this endpoint does not resolve attitude/heliocentric geometry itself). Set to also receive the srp section of the response; omit (or null) to get mass properties only. */
            sun_hat_body?: number[] | null;
            /** @description Heliocentric distance [m], only affects the radiation-pressure magnitude used for srp (not direction). Defaults to 1 AU when omitted. */
            sun_distance_m?: number | null;
        };
        ComponentContribution: {
            /** @description e.g. "Bus/structure (+ propellant, unmodeled subsystems)" or "SolarPanel #0" — the bus/structure remainder is always contributions[0]. */
            label: string;
            mass_kg: number;
            /** @description Relative to the geometric-center origin, NOT the CoM. */
            position_m: number[];
            /** @description This component's own contribution to the total inertia tensor, about the TRUE CoM. Full symmetric 3x3, row-major [[Ixx,Ixy,Ixz],[Ixy,Iyy,Iyz],[Ixz,Iyz,Izz]] — generally non-diagonal for any off-axis component (point-mass parallel-axis form). */
            inertia_about_com_kgm2: number[][];
        };
        SrpPlateForce: {
            normal: number[];
            area_m2: number;
            /** @description Plate centre relative to the geometric-center origin, as built by simulate.rs::build_plates (see VehicleProperties' own frame-consistency note: this is NOT re-expressed relative to the derived CoM). */
            center_body_m: number[];
            /** @description Real per-plate SRP force vector [N], body frame, from orbital_models::flat_plate_force_per_plate — the actual flat-plate law (blends incidence direction and plate normal per rho_s/rho_d), not a magnitude-only or incidence-direction approximation. Exactly [0,0,0] for a plate facing away from the Sun (not omitted from the list). */
            force_n: number[];
        };
        VehicleSrpResult: {
            /** @description The request's sun_hat_body, re-normalized. */
            sun_hat_body: number[];
            /** @description Radiation pressure [N/m^2] used, from sun_distance_m (or 1 AU default). */
            p_srp_nm2: number;
            /** @description Center of pressure: the force-magnitude-weighted centroid of illuminated plates, for THIS sun direction — moves as the sun direction changes, unlike a static geometric centroid. (CoP - CoM) is the real SRP disturbance-torque arm. Relative to the geometric-center origin, same frame as com_m. null only if literally no plate is illuminated (e.g. a degenerate/zero-area config). */
            cop_m?: number[] | null;
            plates: components["schemas"]["SrpPlateForce"][];
            /** @description Sum of every plate's force_n. */
            total_force_n: number[];
        };
        VehicleProperties: {
            /** @description Total spacecraft mass — always equals the request config's spacecraft.mass_kg exactly. */
            mass_kg: number;
            /** @description Center of mass, relative to the geometric-center origin (the bus centroid, [0,0,0] by definition). */
            com_m: number[];
            /** @description Full inertia tensor about the TRUE CoM, row-major, generally non-diagonal (sum of every ComponentContribution's own inertia_about_com_kgm2). Real off-diagonal terms are expected and physically meaningful once any component sits off a principal axis — do NOT assume this reduces to the old hand-typed spacecraft.inertia_diag_kgm2's 3 numbers. UPDATE: the running 6DOF simulation CAN now use this tensor's diagonal — set spacecraft.derive_inertia_from_geometry = true on the request config (see SpacecraftConfig) to make the live sim's inertia match this endpoint's own computation exactly. Off-diagonal (cross-coupling) terms are still not consumed anywhere — SpacecraftProperties remains diagonal-only by signature. Without that flag set, the sim still uses spacecraft.inertia_diag_kgm2 unchanged, and this value stays purely informational for that request. */
            inertia_kgm2: number[][];
            /** @description contributions[0] is always the bus/structure remainder (spacecraft.mass_kg minus every itemized hardware mass, clamped at 0); one entry per [[spacecraft.hardware]] item after that, in config order. */
            contributions: components["schemas"]["ComponentContribution"][];
            /** @description Non-fatal issues, e.g. itemized hardware mass exceeding spacecraft.mass_kg (the bus/structure remainder was clamped to 0 rather than going negative). */
            warnings: string[];
            /** @description Present only when the request set sun_hat_body. */
            srp?: components["schemas"]["VehicleSrpResult"] | null;
            /** @description Review E5, backend half: static main-engine-vs-RCS feasibility. null when the config has no spacecraft.propulsion. */
            main_engine_torque_check?: components["schemas"]["MainEngineTorqueCheck"] | null;
        };
        /** @description Static (no dynamics) feasibility of holding attitude through a main-engine burn: the engine (body +X, mounted at the -X bus face — cruise.rs's fixed convention) produces a disturbance torque r_offset × F_thrust about the CoM (the SAME arm the live simulation flies with, cruise::main_engine_thrust_offset_body_m); RCS must cancel it during ThrustersPrimary. rcs_authority_nm is the placed layout's achievable torque along the cancellation axis (same thruster selection the live allocator uses, full duty, projected onto that axis). If ratio exceeds 1, every burn tumbles the vehicle by physics, not by bug — warn in the Phase 02 builder and before a Phase 03 run. */
        MainEngineTorqueCheck: {
            /** @description Engine mount point minus CoM, body frame [m]. */
            thrust_offset_body_m: number[];
            /** @description |r_offset × F_thrust| [N·m] at the configured propulsion.thrust_n. */
            disturbance_torque_nm: number;
            /** @description Achievable RCS torque along the cancellation axis [N·m]. When the disturbance is zero, the layout's weakest-axis authority over ±x/±y/±z probes instead (a general capability number). 0 with no RCS configured. */
            rcs_authority_nm: number;
            /** @description disturbance_torque_nm / rcs_authority_nm — > 1.0 means this engine/RCS combination cannot hold attitude during a burn. null when a real disturbance exists but the layout has zero authority along the needed axis (worse than any finite ratio). */
            ratio?: number | null;
            /** @description true when there is no disturbance to cancel, or ratio <= 1.0. */
            feasible: boolean;
        };
        /** @description Round-2 slew-test. See SlewTestRequest.initial_state's own description for the semantic switch this triggers. */
        SlewTestInitialStateRequest: {
            /** @description [w, x, y, z], re-normalized server-side. */
            q: number[];
            omega_radps: number[];
        };
        SlewTestRequest: {
            config: components["schemas"]["MissionConfig"];
            /** @description In [0, 180]. Default 90. Role depends on initial_state: with initial_state OMITTED, this offsets the STARTING attitude away from the fixed commanded target (q0 = dq * q_cmd). With initial_state PRESENT, this instead offsets the COMMANDED TARGET away from the fixed nominal (q_cmd = dq * nominal) -- see initial_state's own description for why. */
            initial_error_deg?: number;
            /** @description Rotation axis (inertial frame, re-normalized server-side) paired with initial_error_deg -- see that field's description for which quantity it offsets. Default [0,0,1]. */
            axis?: number[] | null;
            /**
             * @description Default WheelsPrimary. ThrustersOnly (round-2) fully excludes the wheel cluster from control -- zero motor torque, zero reaction, zero momentum-dump activity -- the true RCS-only isolation test (neither WheelsPrimary nor ThrustersPrimary actually isolates RCS: WheelsPrimary lets wheels do the work, ThrustersPrimary still actively drives wheels toward zero speed every tick, a real nonzero reaction torque). See sim_engine::ControlMode.
             * @enum {string|null}
             */
            control_mode?: "WheelsPrimary" | "ThrustersPrimary" | "ThrustersOnly" | null;
            /** @description Quaternion-PD proportional gain. Defaults to the config's own [gnc] reaction_wheel_kp (pd_gains_from_cfg) when omitted. */
            kp?: number | null;
            /** @description Quaternion-PD derivative gain. Defaults to the config's own [gnc] reaction_wheel_kd when omitted. */
            kd?: number | null;
            /** @description Control tick [s]. Default 5.0, clamped server-side to [0.1, 60]. */
            tick_s?: number | null;
            /** @description Simulated duration [s]. Default 1800, clamped server-side to [tick_s, 7200]. */
            duration_s?: number | null;
            /** @description Pointing error below which the response's settling_time_s is recorded. Default 0.5. */
            settle_threshold_deg?: number | null;
            /** @description Round-2. Seeds this call's STARTING attitude/rate directly from a previous call's real ending state (its last sample's q/omega_radps) instead of the canned q0 derivation -- this is what makes a chained sequence of slew-test calls a genuinely continuous 'chase a moving target' animation rather than a jump-cut back to a fresh nominal start every call. When present, initial_error_deg/axis are reinterpreted to offset the COMMANDED TARGET instead of the starting attitude (a fixed target the vehicle already reached would otherwise make every subsequent call start at ~0 error). wheel_speeds_radps and mass_kg are NOT seeded from any prior call -- always the config's own defaults (zero speeds, full spacecraft mass). When set, its own omega_radps takes priority over initial_omega_radps outright (not summed). */
            initial_state?: components["schemas"]["SlewTestInitialStateRequest"] | null;
            /** @description Round-2. Body-frame angular rate [rad/s] to start a FRESH (non-chained) run with, one independent value per axis. Default [0,0,0] (the original behavior). A step response from rest can't surface a control gap on an axis whose initial ERROR happens to start near zero (e.g. a rank-deficient thruster layout that's structurally uncontrollable about one axis); a real, persistent initial SPIN on a specific axis is what actually probes control authority there. Only applied when initial_state is NOT set -- when initial_state IS set, its own real omega_radps (genuine continuity data) takes priority outright, not summed with this field. */
            initial_omega_radps?: number[] | null;
        };
        SlewTestSample: {
            t_s: number;
            error_deg: number;
            wheel_momentum_nms: number;
            /** @description 0-1, this tick's aggregate RCS pulse-width duty cycle (0 unless the tick fired RCS, e.g. ThrustersPrimary/ThrustersOnly mode or a wheel-desaturation pulse). */
            rcs_duty_cycle: number;
            /** @description Real attitude quaternion [w, x, y, z] at this sample -- same convention as every other quaternion field in this API (e.g. SimStepMsg.q, CruiseStepMsg.q). Added for a non-approximated attitude-replay animation of the slew test. */
            q: number[];
            /** @description Real body-frame angular velocity [rad/s] at this sample. */
            omega_radps: number[];
            /** @description Round-2: per-thruster PWM duty cycle [0,1] this tick, indexed IDENTICALLY to the placed-thruster list rcs_from_hardware builds from the request config (zip against that same ordering to know which physical thruster this index is). Every SELECTED (torque-aligned) thruster shares the same nonzero value as rcs_duty_cycle; unselected thrusters are 0.0. Empty array iff the config has no RCS hardware at all. */
            thruster_duty_cycles: number[];
        };
        SlewTestResult: {
            control_mode: string;
            /**
             * @description Three-layer attitude control: the slew test now flies the SAME per-mode registry the cruise loop does (MANUAL.md §10.5), so a ThrustersPrimary/ThrustersOnly test exercises the RCS-authority-derived thruster law. Explicit kp/kd in the request force a plain Pd override.
             * @enum {string}
             */
            controller_law?: "Pd" | "Pid" | "PhasePlane";
            /** @description Effective proportional gain flown; null for PhasePlane. */
            kp: number | null;
            kd: number | null;
            tick_s: number;
            duration_s: number;
            n_ticks: number;
            initial_error_deg: number;
            settle_threshold_deg: number;
            final_error_deg: number;
            /** @description Simulated time [s] at which pointing error first dropped to/below settle_threshold_deg. null if it never settled within duration_s. */
            settling_time_s: number | null;
            /** @description How far above settle_threshold_deg the error climbed again after first settling (0 if it stayed under). null iff settling_time_s is null. */
            overshoot_deg: number | null;
            final_wheel_momentum_nms: number;
            /** @description Peak wheel-cluster momentum magnitude over the run -- compare against the configured wheel cluster's saturation threshold to check for desaturation events. */
            max_wheel_momentum_nms: number;
            rcs_propellant_kg_used: number;
            /** @description Real wall-clock time [ms] the server spent computing this result -- useful for budgeting a future Monte Carlo sweep over many slew tests. */
            wall_clock_ms: number;
            /** @description Round-2: the fixed target attitude [w, x, y, z] every sample's error_deg is measured against for this run -- constant for the whole call (a fixed step test), hence top-level rather than per-sample. See SlewTestSample.q for the actual/live attitude to compare it against. */
            q_command: number[];
            /** @description Decimated time series (capped at 300 points) for plotting. */
            samples: components["schemas"]["SlewTestSample"][];
        };
        StartSimResponse: {
            /** @description Monotonic counter, not a UUID — process-lifetime unique only */
            job_id: number;
        };
        JobStatusResponse: {
            running: boolean;
            /** @description Steps streamed so far. null once done. */
            step?: number | null;
            /** @description Always null in v1 — no upfront estimate is computed. Treat as indeterminate progress, not a percentage. */
            total_steps?: null;
            error?: string | null;
            /** @description true once POST.../cancel has been called for this job (Phase 9k task 4). For a single-run simulate job this reliably means the run stopped early — the truth-integration loop polls the cancellation flag every truth step. For a Monte Carlo job (monte_carlo_runs > 0) this only reflects that cancellation was *requested*: the already-dispatched batch of runs (spawned via thread::scope, joined together) is NOT interrupted mid-flight, a known architectural limitation — see MissionPlanner/src/server/routes/simulate.rs's module doc comment. */
            cancelled: boolean;
        };
        CancelResponse: {
            ok: boolean;
            cancelled: boolean;
        };
        /** @description One streamed step, throttled to measurement cadence (dt_meas_s). Sent as a JSON text frame over the /stream WebSocket. */
        SimStepMsg: {
            t_s: number;
            phase_name: string;
            r_truth_m: number[];
            v_truth_mps: number[];
            r_ekf_m: number[];
            v_ekf_mps: number[];
            /** @description Attitude quaternion [w, x, y, z], body -> inertial/Hill frame */
            q: number[];
            sigma_pos_m: number;
            sigma_vel_mps: number;
            wheel_speeds_radps: number[];
            wheel_momentum_nms: number;
            /** @description 0-1, fraction of max wheel speed (most-saturated wheel) */
            wheel_sat_frac: number;
            desat_fired: boolean;
            /** @description null if OpNav isn't configured */
            bearing_residual_rad?: number | null;
            /** @description null if OpNav isn't configured */
            angular_size_residual_rad?: number | null;
            /** @description null if LIDAR isn't configured or didn't return a measurement this step */
            lidar_residual_m?: number | null;
        };
        SimResult: {
            phases_completed: string[];
            final_r_m: number[];
            final_v_mps: number[];
            dv_total_mps: number;
            /** @description Local filesystem path, not an HTTP-fetchable URL — file serving isn't implemented yet */
            nav_csv_path: string;
            /** @description Local filesystem path, not an HTTP-fetchable URL */
            attitude_csv_path: string;
            /** @description Local filesystem path, not an HTTP-fetchable URL */
            maneuvers_csv_path: string;
        };
        StepsResponse: {
            steps: components["schemas"]["SimStepMsg"][];
        };
        /** @description One body-centric point along a Monte Carlo run's sparse trajectory (~18 points, time-stride-sampled) — deliberately not the dense per-step SimStepMsg feed. */
        McTrajPoint: {
            t_s: number;
            r_truth_m: number[];
        };
        /** @description One streamed message per completed Monte Carlo run, sent as a JSON text frame over /stream when the job was started with monte_carlo_runs > 0 — never a per-truth-step message. */
        McRunMsg: {
            run_index: number;
            seed: number;
            final_r_m: number;
            min_r_m: number;
            max_r_m: number;
            final_sigma_pos_m: number;
            total_dv_ms: number;
            crashed: boolean;
            /** @description Present only for a stride-selected subset of runs (capped at 20 regardless of monte_carlo_runs); null for all other runs. */
            trajectory?: components["schemas"]["McTrajPoint"][] | null;
        };
        /** @description Final result for a Monte Carlo job (monte_carlo_runs > 0), analogous to SimResult for a single-run job. */
        McSummaryResult: {
            n_runs: number;
            n_crashed: number;
            mean_final_r_m: number;
            min_r_m: number;
            max_r_m: number;
            mean_dv_total_ms: number;
            /** @description Local filesystem path, not an HTTP-fetchable URL */
            mc_summary_csv_path: string;
        };
        McRunsResponse: {
            runs: components["schemas"]["McRunMsg"][];
        };
        /** @description One SOI-candidate or third-body-perturber entry for the optimization stage's force model — the user-specified equivalent of what /api/design/trajectory infers automatically from target_body/third_bodies. */
        OptimizationBody: {
            /** @description Catalog body name — see /api/bodies. */
            name: string;
            /**
             * @description CentralWhenInSoi: this body becomes the propagator's central body whenever the spacecraft is inside its Laplace sphere of influence (real SOI-switching). AlwaysThirdBody: always a point-mass perturber, never a central-body candidate.
             * @enum {string}
             */
            role: "CentralWhenInSoi" | "AlwaysThirdBody";
            /**
             * @description Central-body gravity fidelity, only meaningful when role = CentralWhenInSoi. Must be null/omitted for AlwaysThirdBody (third-body perturbers are point-mass only by design). Numeric j2/j3/j4 values always come from the body_models catalog, not user-supplied — falls back to PointMass with a server-side warning if the catalog lacks matching harmonic/pole data for the body.
             * @enum {string|null}
             */
            fidelity?: "PointMass" | "J2" | "J2J3J4" | "SphericalHarmonic" | null;
        };
        ForceModelConfig: {
            /** @description Default false. Not yet wired into the Phase 9 fitness evaluator — reserved for a future fidelity upgrade. */
            srp?: boolean;
            /**
             * @description Only Dopri5 is actually implemented by the underlying propagator today; other values are accepted by the schema but not yet honored.
             * @enum {string}
             */
            integrator: "RK4" | "DormandPrince45" | "Dopri5" | "Radau";
            rtol: number;
            atol: number;
            /**
             * @description Additional third-body perturbers / SOI candidates beyond the departure and target bodies, which are *always* auto-registered as CentralWhenInSoi candidates regardless of this list. May be omitted or empty — no longer required to be non-empty.
             * @default []
             */
            bodies: components["schemas"]["OptimizationBody"][];
        };
        /** @description Required when optimization.method = "GA". No defaults — every field must be supplied. */
        GaParams: {
            population_size: number;
            generations: number;
            crossover_rate: number;
            /** @description Peak per-gene mutation probability — decays via cosine schedule to 10% of this value by the final generation. */
            mutation_rate: number;
            elitism_count: number;
        };
        /** @description Required when optimization.method = "PSO". No defaults — every field must be supplied. */
        PsoParams: {
            swarm_size: number;
            iterations: number;
            inertia_weight: number;
            cognitive_weight: number;
            social_weight: number;
        };
        /** @description Required when optimization.method = "MultipleShooting". Schema-only today — the method itself is not implemented yet and the job will fail at run time with a clear error. */
        ShootingParams: {
            max_iterations: number;
            tolerance: number;
        };
        /** @description Tisserand beam-search configuration for automatic intermediate-body discovery (Phase 9j). When present under optimization.mga.sequence_search, the optimizer discovers flyby body sequences automatically from candidate_bodies rather than requiring the user to specify flyby_bodies explicitly. The outer Tisserand beam search prunes the discrete sequence space; the inner DE optimizer runs on each top-ranked candidate sequence. mga.flyby_bodies is ignored when sequence_search is set. */
        SequenceSearchConfig: {
            /** @description Pool of body names to consider as intermediate flyby targets. Each must be a catalog body with a known semi-major axis. Repeated visits (e.g. Venus→Earth→Earth→Jupiter) are allowed — list each body once. */
            candidate_bodies: string[];
            /** @description Maximum number of legs (= max intermediate flybys + 1). Keep ≤ 6 for reasonable run time. */
            max_legs: number;
            /** @description How many partial sequences to keep alive at each depth level of the Tisserand tree. Larger = more thorough outer search. Default: 25. */
            beam_width?: number;
            /** @description How many top-ranked Tisserand sequences to run the full inner DE optimizer on. Default: 8. */
            max_sequences_to_optimize?: number;
            /** @description Approximate departure v∞ [m/s] used to seed the Tisserand graph walk. A rough estimate is fine; the inner optimizer finds the true value. Default: 5000. */
            vinf_departure_estimate_ms?: number;
        };
        /** @description Required when optimization.method = "MGA". Implements MGA-1DSM (one deep-space manoeuvre per leg) with Differential Evolution/SHADE/MBH as the inner optimizer (Phase 9d-9g, 9x-v). The full leg sequence is [optimization.departure_body] + flyby_bodies + [optimization.target_body]. Set sequence_search to have the optimizer discover flyby bodies automatically via Tisserand beam search (Phase 9j) instead of specifying them manually. */
        MgaParams: {
            /** @description Intermediate bodies only, in visit order — excludes the overall departure_body/target_body. Ignored when sequence_search is set. Optional — defaults to [] when omitted (Phase 9k-v bug #2 fix: previously omitting this field entirely caused a 400 with an opaque JSON parse error, even though it was already documented as optional here). */
            flyby_bodies?: string[];
            /** @description One [min, max] TOF range [days] per leg. When sequence_search finds fewer legs than entries here, the last entry is used as a fallback. */
            leg_tof_days: number[][];
            /** @description Maximum allowed departure v∞ from the departure body [m/s]. Default: 15000. */
            departure_vinf_max_ms?: number;
            /** @description Minimum allowed departure v∞ from the departure body [m/s] (Phase 9v-iii). Default 0.0 = unconstrained. Must satisfy 0 <= min < departure_vinf_max_ms. Prevents degenerate near-zero departure burns (the GTOP Cassini-2 benchmark bounds departure v∞ to [3, 5] km/s). */
            departure_vinf_min_ms?: number;
            /** @description Minimum periapsis radius [m] for any intermediate gravity-assist flyby. Must be > flyby body surface radius. Default: 6371000 (Earth radius). */
            flyby_min_periapsis_m?: number;
            /** @description Maximum acceptable arrival v∞ at the final body [m/s]. Candidates exceeding this are penalised in the real-objective phase. Default: 25000. */
            arrival_vinf_max_ms?: number;
            /** @description Per-leg [min, max] periapsis bound override (units of that flyby body's radius), one entry per intermediate flyby (flyby_bodies.length entries, one fewer than leg_tof_days). Default null, which keeps the historical uniform (1.05, 300.0) bound for every flyby regardless of body. Added to let a specific benchmark config (e.g. the real GTOP Cassini-2 problem, whose official bounds per Izzo & Vinkó 2008 Table IV differ sharply per body: Venus [1.05, 6], Earth [1.15, 6.5], Jupiter [1.7, 291]) reproduce the exact official search box instead of the generic, much wider default. */
            rp_norm_bounds?: number[][] | null;
            /** @description Upper bound override for every leg's DSM-timing fraction η. Default null, which keeps the historical 0.99 upper bound. The real GTOP Cassini-2 problem uses 0.9 (Table IV) — set this to 0.9 to match exactly when reproducing that benchmark. Added alongside rp_norm_bounds for the same reason. */
            eta_max?: number | null;
            /** @description DE mutation scale factor F ∈ [0.4, 1.0]. Default: 0.8. */
            de_f_weight?: number;
            /** @description DE crossover probability CR ∈ [0.1, 1.0]. Default: 0.9. */
            de_cr?: number;
            /** @description DE population size. Default: 300. */
            de_population_size?: number;
            /** @description Total DE generation budget (shared across phase 1 restarts and phase 2). Default: 1000. */
            de_generations?: number;
            /** @description Number of independent phase-1 restarts. Default: 3. */
            de_restarts?: number;
            /** @description Base RNG seed for the DE search (Phase 9v-vii). Restart r uses de_seed + r*137; phase 2 uses de_seed + de_restarts*137. Default 42 reproduces the historical hardcoded seeding. Vary across runs to measure seed-to-seed spread. */
            de_seed?: number;
            /** @description Use SHADE self-adaptive DE (success-history F/CR adaptation + current-to-pbest/1 with archive; Tanabe & Fukunaga 2013) instead of classic fixed-parameter DE/rand/1/bin. When true, de_f_weight/de_cr seed the success-history memory rather than being fixed. Default: true. */
            de_adaptive?: boolean;
            /** @description When set, auto-discovers flyby body sequences via Tisserand beam search instead of using flyby_bodies. */
            sequence_search?: components["schemas"]["SequenceSearchConfig"] | null;
            /** @description Minimum heliocentric perihelion [m] permitted for either sub-arc of any leg (the departure Kepler sub-arc or the Lambert sub-arc) — rejects chromosomes whose orbit dives unrealistically close to the Sun. Distinct from flyby_min_periapsis_m (closest approach to a flyby body, not the Sun). Default: 0.2 AU (2.991957414e10 m). */
            min_solar_perihelion_m?: number;
            /** @description Phase 9w ballistic (no-DSM) powered-flyby grid scan config — the middle tier between Tisserand sequence pruning and the DE-based MGA-1DSM search. Optional: when present, the mga-scan CLI subcommand (and POST /api/mga-scan) can run a deterministic launch-window scan for this mission's flyby sequence before (or instead of) running the DE/MBH search. */
            scan?: components["schemas"]["MgaScanConfig"] | null;
            /** @description Phase 9x incremental pruning (Ceriotti 2010, Ch. 3) — an optional leg-by-leg pre-search that seeds Phase 1's DE restarts instead of (or alongside) the existing seeded/random restarts. Optional: when absent, run_mga behaves exactly as before (no pruning). */
            pruning?: components["schemas"]["PruningConfig"] | null;
            /** @description Phase 9x-v Stage 3: which global search algorithm runs Phase 2 (the real-objective refinement). Mbh (Monotonic Basin Hopping, default as of Stage 4) beat matched-budget DE by 40-57% on both head-to-head validation missions (VEEGA 1989, Cassini-2 GTOP) — see docs/MP/BENCHMARKS.md "Search method comparison". De remains available as an explicit opt-in for A/B comparison. Default: Mbh. */
            search_method?: components["schemas"]["SearchMethod"];
            /** @description MBH configuration, used only when search_method = "Mbh". Optional — omitting takes the documented defaults. */
            mbh?: components["schemas"]["MbhConfig"] | null;
        };
        /**
         * @description Which global search algorithm runs MGA-1DSM Phase 2 (Phase 9x-v Stage 3). See trajectory_solver::mbh for the algorithm itself. Default: Mbh.
         * @enum {string}
         */
        SearchMethod: "De" | "Mbh";
        /** @description Configuration for the Phase 9x-v Monotonic Basin Hopping search method. See trajectory_solver::MbhSolver for the algorithm itself. All fields optional — every field has a documented default, so an empty object is valid. */
        MbhConfig: {
            /** @description Number of hops per chain (after each chain's initial local descent). Default: 200. */
            hops?: number;
            /** @description Fraction of chromosome variables perturbed on each kick. Default: 0.3. */
            perturb_fraction?: number;
            /** @description Kick magnitude as a fraction of each variable's bounds width. Default: 0.15. */
            kick_scale?: number;
            /** @description Max local-descent iterations per hop (both the initial descent and every post-kick refinement), for whichever local_optimizer is selected. Default: 500. */
            local_max_iter?: number;
            /** @description RNG seed for the kick sequence. Default: 42. */
            seed?: number;
            /** @description Early-stop-on-stagnation: a chain terminates after this many consecutive non-improving hops instead of always burning its full hops budget. null (default) disables early stop. Added per the real pagmo2 MBH default (stop = 5, src/algorithms/mbh.cpp). */
            stop_after?: number | null;
            /** @description Which local optimizer runs MBH's inner descent step. NelderMead (default, unchanged prior behaviour), CompassSearch (a best-of-all-directions pattern search, added), or HookeJeeves (a greedy accept-first-improving pattern search with an evaluation-budget termination, added — real MBH reference implementations default to a very shallow per-hop descent of this kind, not a full local solve). */
            local_optimizer?: components["schemas"]["MbhLocalOptimizer"];
            /** @description HookeJeevesSearch configuration, used only when local_optimizer = "HookeJeeves". Defaults reproduce a real, independently-verified MBH reference implementation's own default inner-descent behaviour exactly (a fresh reimplementation, not a port — see the Third-Party Algorithm Reimplementation Policy). */
            hooke_jeeves?: components["schemas"]["HookeJeevesConfig"] | null;
            /** @description Extra purely-randomly-initialised chains added alongside the elite seeds (pruning survivors + bidirectional-backfit stitches). 0 (default) is byte-identical to prior behaviour. Added: when non-zero, the outer search is no longer 100% dependent on the pre-search having found the right basin. */
            extra_random_chains?: number;
            /** @description Global-relative early stop: abandon a chain once past global_stall.patience hops if it's still worse than the best result any chain has found so far by more than global_stall.margin_frac. null (default) disables this — byte-identical to prior behaviour. Added after finding that stop_after alone lets a chain keep dodging its own consecutive-failure counter indefinitely while being globally irrelevant. */
            global_stall?: components["schemas"]["GlobalStallConfig"] | null;
            /** @description Archipelago-style migration: every migration.interval of a chain's own hops, a chain whose incumbent is worse than the live global best by more than migration.margin_frac receives the global best as its new incumbent and keeps hopping from there with its own kick stream. Where global_stall kills a losing chain, migration repurposes it. Promoted to a standard default (enabled = true) after a same-seed A/B on Cassini-2 showed it strictly helped two of three resonance-family branches and never hurt the winning branch — see the design notes Phase 9x-v. Set enabled = false to restore previous behaviour exactly. */
            migration?: components["schemas"]["MigrationConfig"];
        };
        /**
         * @description Which local optimizer runs MbhSolver's inner descent step. See MbhConfig.local_optimizer. Default: NelderMead.
         * @enum {string}
         */
        MbhLocalOptimizer: "NelderMead" | "CompassSearch" | "HookeJeeves";
        /** @description Configuration for trajectory_solver::HookeJeevesSearch. See MbhConfig.hooke_jeeves. All fields optional with real reference-implementation defaults. */
        HookeJeevesConfig: {
            /** @description Fitness-evaluation budget per local descent (NOT an iteration count). 1 (default) is a real, independently-verified reference-implementation default, not a placeholder — it produces a near-single-probe descent, deliberately leaving nearly all exploration work to the outer MBH hop loop. */
            max_fevals?: number;
            /** @description Initial step size, as a fraction of each parameter's bounds width. Default: 0.1. */
            start_range?: number;
            /** @description Stop once the (normalised) step size drops below this. Default: 0.01. */
            stop_range?: number;
            /** @description Step shrink factor applied after a full sweep finds no improving move. Default: 0.5. */
            reduction_coeff?: number;
        };
        /** @description Configuration for trajectory_solver::MbhSolver's global-relative early stop. See MbhConfig.global_stall. Both fields required when this object is provided (no defaults). */
        GlobalStallConfig: {
            /** @description Hops a chain is allowed past which it is abandoned if still worse than the live global best by more than margin_frac. */
            patience: number;
            /** @description Fractional margin (relative to the live global best) beyond which a past-patience chain is considered globally irrelevant and abandoned. */
            margin_frac: number;
        };
        /** @description Configuration for trajectory_solver::MigrationConfig. See MbhConfig.migration. All fields optional. */
        MigrationConfig: {
            /** @description true (default) = migration active with the parameters below. Set false to disable — reproduces previous behaviour. */
            enabled?: boolean;
            /** @description How often (in a chain's own hops) a migration check runs. Default: 10. */
            interval?: number;
            /** @description Fractional margin (relative to the live global best) beyond which a chain is migrated onto the global best. Default: 0.3. */
            margin_frac?: number;
        };
        /** @description Configuration for the Phase 9x incremental pruning pre-search. See crates/trajectory_solver/src/incremental_pruning.rs for the generic engine and the design notes Phase 9x section for the motivation. All fields optional. */
        PruningConfig: {
            /** @description Number of random samples drawn at level 0 (departure + leg-0 vars). Default: 2000. */
            samples_level0?: number;
            /** @description Number of random continuations sampled per surviving parent at every level after 0. Default: 50. */
            children_per_survivor?: number;
            /** @description Hard cap on survivors carried into the next level (bounds total evaluation count — keeps the search polynomial in level count). Default: 40. */
            max_survivors_per_level?: number;
            /** @description Pruning threshold as a multiple of the best partial cost found at that level. "Generous" is intentional (Ceriotti's own term) — this discards clearly-bad prefixes, not just the single best one. Default: 3.0. */
            threshold_factor?: number;
            /** @description Number of best survivors (by partial cost) fed as elite seeds into the existing Phase 1 DE restarts. Default: 8. */
            n_seeds?: number;
            /** @description Bidirectional ("meet in the middle") extension (Phase 9x): alongside forward leg-by-leg pruning, run a mirror-image backward pass from the target and inject the best stitched full chromosomes as additional Phase 2 seeds — generic promotion of the backward-fit decomposition that hand-solved Galileo's VEEGA. Default: true; set false for A/B isolation runs. */
            bidirectional?: boolean;
            /** @description Admissible remaining-cost bound (branch-and-bound extension): before the forward pruning pass, run a small backward suffix sampling per intermediate split and bin the cheapest suffix cost found; the forward pass then prunes on cost_so_far + bound (A*'s f = g + h) instead of cost-so-far alone (the GASP mechanism, Myatt et al. 2004; Izzo et al. 2007). null (default) = today's purely greedy behaviour, byte-identical. Deliberately NOT promoted to a default (unlike surrogate/migration) — its own A/B was mixed and a bound+surrogate stack regressed further as budget grew — see the design notes Phase 9x-v. */
            bound?: components["schemas"]["PruningBoundConfig"] | null;
            /** @description Surrogate-assisted child sampling: reuse the evaluations pruning already performs by fitting a per-level RBF model to (child vars → added cost) pairs and drawing several candidate children per slot, keeping only the best-predicted one for real evaluation (Jones et al. 1998 EGO paradigm; Regis & Shoemaker 2007 RBF variant). Promoted to a standard default (enabled = true) after beating plain sampling on every tested branch — see the design notes Phase 9x-v. Set enabled = false to restore previous (plain-sampling) behaviour exactly. */
            surrogate?: components["schemas"]["PruningSurrogateConfig"];
        };
        /** @description Configuration for the pruning remaining-cost bound. See PruningConfig.bound. All fields optional. */
        PruningBoundConfig: {
            /** @description Fraction of the sampled suffix-cost bound that is discounted before use, hedging the gap between a sampled minimum (an overestimate of the true minimum) and an admissible bound: h = bin_min * (1 - safety_frac). 0.0 trusts the sampled minimum outright; larger is safer but prunes less. Default: 0.5. */
            safety_frac?: number;
            /** @description Bins over the interface-epoch axis of each split's bound table. Default: 24. */
            epoch_bins?: number;
            /** @description Bins over the interface-|v∞| axis of each split's bound table. Default: 12. */
            vinf_bins?: number;
            /** @description Backward samples drawn per split to build the table (full-length suffixes, cheapest-per-bin retained). Default: 3000. */
            samples_per_split?: number;
        };
        /** @description Configuration for surrogate-assisted pruning sampling. See PruningConfig.surrogate. All fields optional. */
        PruningSurrogateConfig: {
            /** @description true (default) = surrogate-assisted sampling active. Set false to disable — reproduces previous (plain-sampling) behaviour. */
            enabled?: boolean;
            /** @description Candidate children drawn per child slot; only the best-predicted one is really evaluated. Default: 4. */
            oversample_factor?: number;
            /** @description Minimum recorded (vars, cost) pairs at a level before the surrogate is trusted; below this, sampling is plain (first candidate kept). Default: 60. */
            min_fit_samples?: number;
            /** @description Gaussian-kernel length scale in normalized [0,1] units. Default: 0.3. */
            length_scale?: number;
        };
        /** @description Configuration for the Phase 9w ballistic (no-DSM) powered-flyby MGA grid scan. Scans a departure-date x per-leg-TOF grid with pure Lambert legs and analytic powered-flyby feasibility (no DSMs, no propagation, no optimizer) — see the design notes Phase 9w and trajectory_solver::mga_scan for the model. */
        MgaScanConfig: {
            /** @description Scan horizon centered on optimization.departure_epoch, spanning ± horizon_years/2 years. */
            horizon_years: number;
            /** @description Departure-date grid step [days]. Default: 5.0. */
            departure_step_days?: number;
            /** @description Number of TOF grid points per leg, spanning that leg's optimization.mga.leg_tof_days bounds. Default: 40. */
            tof_grid_points_per_leg?: number;
            /** @description Prune branches whose per-flyby powered-flyby matching burn exceeds this [m/s] — the scan's "v∞-match tolerance" expressed as a ΔV cost. Default: 3000. */
            flyby_dv_max_ms?: number;
            /** @description Safety cap on stored feasible branches. Default: 500000. */
            max_records?: number;
        };
        StartMgaScanResponse: {
            /** @description Monotonic counter, not a UUID — process-lifetime unique only. Separate counter from /api/simulate's and /api/optimize's job IDs. */
            job_id: number;
        };
        MgaScanJobStatusResponse: {
            running: boolean;
            error?: string | null;
        };
        /** @description One feasible complete branch found by the scan — mirrors one row of mga_window_scan.csv. */
        MgaScanRecordApi: {
            /** @description Departure epoch, days since the MJD2000 epoch (JD 2451544.5). */
            dep_mjd2000: number;
            /** @description Departure hyperbolic excess speed [m/s]. */
            vinf_dep_ms: number;
            /** @description Arrival hyperbolic excess speed at the final target body [m/s]. */
            vinf_arr_ms: number;
            /** @description Sum of flyby_dvs_ms — the branch's total powered-flyby matching-burn cost [m/s]. */
            sum_flyby_dv_ms: number;
            /** @description Vis-viva capture burn from vinf_arr_ms into [trajectory.capture]'s configured orbit around the target body — null when that section/field isn't set (e.g. a Flyby mission has no orbit to capture into). Total cost is vinf_dep_ms + sum_flyby_dv_ms for a Flyby-style trajectory (no arrival burn), or + capture_dv_ms for an Orbit-style trajectory (with a real capture burn) — the caller picks which, since both are legitimate depending on mission type. */
            capture_dv_ms?: number | null;
            /** @description Sum of leg_tofs_days [days]. */
            total_tof_days: number;
            /** @description One entry per leg, leg order (departure_body -> flyby_bodies... -> target_body). */
            leg_tofs_days: number[];
            /** @description Powered-flyby matching burn at each intermediate body [m/s] — one entry per flyby body (length = body_sequence.length - 2). */
            flyby_dvs_ms: number[];
            /** @description Solved shared periapsis radius at each flyby [km]. */
            flyby_rp_km: number[];
            /** @description Total turn angle achieved at each flyby [deg]. */
            flyby_turn_deg: number[];
        };
        MgaScanApiResult: {
            /** @description [departure_body, flyby_bodies..., target_body], in visit order. */
            body_sequence: string[];
            n_legs_evaluated: number;
            /** @description Feasible branches beyond optimization.mga.scan.max_records, discarded rather than returned. */
            n_records_dropped: number;
            records: components["schemas"]["MgaScanRecordApi"][];
        };
        /** @description One candidate flyby sequence found by the Tisserand beam search. */
        RankedSequenceApi: {
            /** @description Intermediate flyby bodies only, in visit order (excludes the departure and target bodies, which are the chain's overall endpoints). Empty for the direct (zero-flyby) case. */
            flyby_bodies: string[];
            /** @description Estimated v∞ at the target body from the Tisserand graph walk [m/s] — a rough estimate; the inner DE/scan finds the true value. */
            estimated_vinf_arr_ms: number;
            /** @description Cumulative Tisserand feasibility score (lower = better). This is the list's sort key — `sequences` is always sorted ascending by this field. */
            tisserand_score: number;
        };
        SequenceSearchApiResult: {
            /** @description Every candidate sequence the beam search found, sorted by tisserand_score ascending (best first) — not just the top pick. */
            sequences: components["schemas"]["RankedSequenceApi"][];
        };
        /** @description Phase 9 trajectory optimization stage (Layer 1b) — real propagated-dynamics optimization, distinct from trajectory's closed-form narrowing-stage solvers (Hohmann/Lambert/GridSearch/MonteCarlo/GA/PSO). Carries its own independent copy of departure epoch/window fields, NOT derived from `trajectory`'s. The departure burn is NOT anchored to a Lambert-solved v-infinity (that would make the GA/PSO search trivially converge to the analytic Hohmann-like minimum — not a meaningful test of global search). Instead it's four independent, freely-searchable numbers: departure_offset_days (which day — sets the departure/target synodic alignment), and three burn parameters at a circular parking orbit: burn location (true anomaly, full range, no config knob), burn magnitude (dv_min_ms..dv_max_ms), and an out-of-plane angle (full range — handles the real plane change to the target's orbit). Reaching the target at all is therefore a genuine search outcome, not a foregone conclusion. */
        OptimizationConfig: {
            /**
             * @description Distinct from mission.objective (Flyby/Orbit/Landing/etc, the MISSION objective) — this is the scalar the Phase 9 optimizer minimizes under real propagated dynamics. MinC3/MaxMassMargin (originally 4th/5th options) were dropped: for a fixed spacecraft, both are deterministic monotonic functions of the same total ΔV MinDeltaV already uses, carrying zero independent optimization signal. MatchTargetDistance minimizes the difference between the real closest-approach distance to the target body and trajectory.capture.target_orbit_radius_m (required when this objective is selected) — the right objective for Flyby/Orbit, where the goal is a specific flyby/capture distance, not hitting the target exactly.
             * @enum {string}
             */
            objective: "MinDeltaV" | "MinTof" | "MatchTargetDistance";
            /**
             * @description Distinct from trajectory.solver's GA/PSO (closed-form Lambert-proxy search) — these variants evaluate real propagated dynamics inside the fitness loop, a materially different cost profile despite the shared algorithm name. GA and PSO use real-propagated single-leg dynamics; MGA uses the MGA-1DSM model (Phase 9d-9j, fully implemented) with a selectable Phase-2 global search algorithm (mga.search_method: DE/SHADE or MBH — Monotonic Basin Hopping, the default since Phase 9x-v Stage 4). MultipleShooting is recognized but not implemented yet and will fail at job run time with a clear error.
             * @enum {string}
             */
            method: "GA" | "PSO" | "MultipleShooting" | "MGA";
            /** @description Catalog body name (must be ANISE-covered — see /api/bodies). Also the start of the MGA leg chain when method = "MGA". */
            departure_body: string;
            /** @description Catalog body name (must be ANISE-covered). Also the end of the MGA leg chain when method = "MGA" — intermediate flyby bodies are configured separately under mga.flyby_bodies. */
            target_body: string;
            /** @description ISO 8601, e.g. "2026-09-15T00:00:00 UTC". Required for the job to actually run (enforced at run time, not at /api/validate). */
            departure_epoch?: string | null;
            /** @description Search window width [days] centered on departure_epoch. Omit/null for a fixed departure epoch (window = 0). */
            departure_window_days?: number | null;
            /** @description Minimum departure burn magnitude to search [m/s]. */
            dv_min_ms: number;
            /** @description Maximum departure burn magnitude to search [m/s]. */
            dv_max_ms: number;
            /** @description Burn-location angle theta search range minimum [deg] — user-configurable search-variable ranges. Defaults to 0 (with theta_max_deg defaulting to 360: the full circle, the previous hardcoded behavior). check_config requires 0 <= min < max <= 360. */
            theta_min_deg?: number | null;
            /** @description Burn-location angle theta search range maximum [deg] — see theta_min_deg. */
            theta_max_deg?: number | null;
            /** @description Out-of-plane angle phi search range minimum [deg] — defaults to -45 (with phi_max_deg defaulting to +45, the previous hardcoded range). check_config requires -90 <= min < max <= 90. */
            phi_min_deg?: number | null;
            /** @description Out-of-plane angle phi search range maximum [deg] — see phi_min_deg. */
            phi_max_deg?: number | null;
            /** @description How long to coast and watch for a close approach to the target body before giving up [days] — a ceiling, not a prescribed transfer duration. The achieved time-to-target is an output of the search (see OptimizeApiResult.achieved_tof_days), not an input. */
            max_coast_days: number;
            force_model: components["schemas"]["ForceModelConfig"];
            /** @description Required when method = "GA". */
            ga?: components["schemas"]["GaParams"] | null;
            /** @description Required when method = "PSO". */
            pso?: components["schemas"]["PsoParams"] | null;
            /** @description Required when method = "MultipleShooting" (not implemented yet). */
            shooting?: components["schemas"]["ShootingParams"] | null;
            /** @description Required when method = "MGA". Configures the MGA-1DSM optimizer (DE inner solver, two-phase search, optional Tisserand sequence discovery). */
            mga?: components["schemas"]["MgaParams"] | null;
        };
        StartOptimizeResponse: {
            /** @description Monotonic counter, not a UUID — process-lifetime unique only. Separate counter from /api/simulate's job IDs. */
            job_id: number;
        };
        OptimizeJobStatusResponse: {
            running: boolean;
            /** @description Generations (GA) or iterations (PSO) completed so far. null once done. */
            step?: number | null;
            /** @description Known upfront from optimization.ga.generations or optimization.pso.iterations — unlike /api/simulate's status, this is usually a real number, not always null. null for MGA (whose step count is determined dynamically by de_restarts × de_generations) and for MultipleShooting. */
            total_steps?: number | null;
            error?: string | null;
            /** @description true once POST.../cancel has been called for this job (Phase 9k task 4). Honored by GA/PSO (checked every generation/iteration, plus every pass of the post-search coordinate-descent refinement) — the job stops within roughly one generation and /result returns whichever candidate was best so far, not an error. NOT honored by MGA (method = "MGA") — its DE search has no cancellation hook yet; cancelled still flips true, but the in-flight compute runs to completion regardless. See MissionPlanner/src/optimize.rs's optimize_api_with_progress doc comment. */
            cancelled: boolean;
        };
        /** @description One streamed step — sent as a JSON text frame over the /stream WebSocket. Deliberately no propagated arc (see the design notes Monte Carlo streaming rule, applied here too) — the arc is computed once, in /result. */
        OptimizeStepMsg: {
            /** @description 0-indexed generation (GA) or iteration (PSO). Since (GA): ONE unified generation counter that never exceeds the configured [optimization.ga].generations budget — the internal restart structure's gen-0 callbacks re-emit the current index instead of inflating it, and the post-search coordinate-descent refinement no longer streams steps at all (its polish still lands in /result). A client can safely set its x-axis to the configured budget. */
            step: number;
            /** @description 1 (GA's internal closest-approach bootstrap phase), 2 (real-objective refinement; PSO always 2), or 3 (the post-search coordinate-descent refinement pass, all emitted at step = the configured budget -- render distinctly, e.g. red). Since informational/diagnostic only: best_fitness is on ONE unit across phases. */
            phase: number;
            /** @description Best-so-far value of the configured objective in its NATURAL units (since second revision same day — no Hohmann normalization on the wire): MinDeltaV → total ΔV [m/s]; MinTof → achieved TOF [days]; MatchTargetDistance → |closest − target| [km]. Selection internally still uses the normalized fitness; this is the displayable value of the same best candidate. f64::MAX serialization (~1.8e308) is the 'nothing feasible yet' sentinel, same as before. For MGA jobs, unchanged: total ΔV [m/s]. */
            best_fitness: number;
            /** @description The best decision-variable vector found so far. MGA (method = "MGA"): the raw MGA chromosome (see mga.rs's chromosome layout doc comment), Phase 9k task 1. GA/PSO (since previously always null for these methods): the 4-vector [dep_offset_days, theta_burn_rad, dv_departure_ms, phi_out_of_plane_rad] matching bounds_from's parameter order — the live search-variable convergence plot's data source. null only on early steps before any feasible candidate exists. CORRECTION (Phase 9k "step-stream context"): under a FIXED flyby_bodies sequence (sequence_search not set) the sequence is constant for the whole job and a client only needs departure offset + per-leg TOF/eta to decode a schematic path. Under sequence_search auto-discovery, the sequence is NOT constant — several candidate sequences are optimized in turn within one job — so a client MUST track the most recent MgaSequenceContextMsg to know which real flyby_bodies these params decode against. */
            best_params?: number[] | null;
            /** @description Per-leg trajectory state (DSM position/velocity, arrival velocity, arrival v-infinity) for best_params, one entry per leg in leg order — MGA (method = "MGA") only, backlog item #18. Every quantity here is already computed as an ordinary byproduct of chromosome fitness evaluation (mga_leg.rs's MgaLegResult); it is re-extracted once per streamed generation (not once per fitness evaluation) so a live-replay client can draw the REAL solved Lambert arc / DSM point / flyby geometry for the best-so-far chromosome, instead of client-side reconstructing an approximate, Lambert-branch-blind re-solve from best_params alone. null for GA/PSO (no leg concept). For an MGA job this can still be an empty array on a generation whose best chromosome happened not to be evaluable — treat that the same as "no leg detail this generation", not as a different job type. */
            mga_legs?: components["schemas"]["MgaLegStepInfo"][] | null;
            /** @description This generation's full evaluated population (each entry one parameter vector, same [dep_offset_days, theta_burn_rad, dv_departure_ms, phi_out_of_plane_rad] layout as best_params) — GA jobs only: the live search-space scatter plots' data source, so the full generation clouds render DURING the run, not only after completion via population_log. FEASIBLE individuals only, parallel to population_outcomes. null for PSO (its solver exposes only the global best per iteration) and MGA. */
            population?: number[][] | null;
            /** @description Per-individual outcome pairs [dv_arrival_ms, tof_days], parallel to (same length/order as) population — GA jobs only: the outcome scatter plots' data source (departure vs arrival ΔV, departure offset vs coast time). Recorded as fitness-evaluation side-products, zero extra propagation cost. null whenever population is null. */
            population_outcomes?: number[][] | null;
            /** @description Best objective value among FEASIBLE-capture candidates seen so far, scanned across every individual of every population (natural units, same as best_fitness) —: null until the first candidate whose approach periapsis lies inside the target's SOI exists. The convergence plot's solid line; best_fitness is the dashed may-be-infeasible lower bound (a borderline-outside-SOI candidate can hold the fitness lead while feasible solutions exist elsewhere in the population). For MGA: mirrors best_fitness. */
            best_feasible_value?: number | null;
        };
        /** @description Per-leg trajectory state for one evaluated MGA chromosome, extracted from mga_leg.rs's MgaLegResult (backlog item #18). Streamed inside OptimizeStepMsg.mga_legs for MGA jobs only. */
        MgaLegStepInfo: {
            /** @description Spacecraft position at the deep-space maneuver point [m], heliocentric [x, y, z]. */
            r_dsm_m: number[];
            /** @description Spacecraft velocity just after the DSM [m/s], heliocentric — the Lambert-arc departure velocity that seeds segment B of this leg. */
            v_dsm_after_mps: number[];
            /** @description Spacecraft heliocentric velocity at leg arrival (Lambert arc endpoint, before subtracting the next body's velocity). */
            v_arrival_helio_mps: number[];
            /** @description Hyperbolic excess velocity on arrival relative to the next body [m/s] — feeds the flyby turn at an intermediate body, or the arrival v-infinity at the final target. Useful for a live flyby-turn indicator. */
            v_inf_arr_mps: number[];
        };
        /** @description A separate announcement frame, NOT a step — interleaved into the same /stream WebSocket as OptimizeStepMsg (Phase 9k "step-stream context" ask). Fires once per candidate flyby-body sequence for an MGA job, sent BEFORE the OptimizeStepMsg frames for that sequence's own generations arrive. Never sent for GA/PSO jobs. Distinguish from OptimizeStepMsg on the wire by the presence of msg_type — OptimizeStepMsg never carries that field, so no existing consumer parsing OptimizeStepMsg directly is affected by this addition. */
        MgaSequenceContextMsg: {
            /**
             * @description Always "mga_sequence" — the discriminant that distinguishes this frame from OptimizeStepMsg.
             * @enum {string}
             */
            msg_type: "mga_sequence";
            /** @description 0-indexed position of this sequence among the candidates being optimized this run. */
            seq_idx: number;
            /** @description Total candidate sequences being optimized this run (>= 1; also 1 for the non-sequence_search fixed-sequence path). */
            seq_count: number;
            /** @description The real flyby-body sequence about to be optimized (excludes the overall departure_body/target_body — same convention as MgaParams.flyby_bodies / the interior of OptimizeApiResult.mga_body_sequence). */
            flyby_bodies: string[];
            /** @description True for the always-included zero-flyby direct baseline the Tisserand beam search compares every gravity-assist candidate against (Phase 9j-B). False for every other sequence, including the fixed-sequence (non-auto) path. */
            is_direct_baseline: boolean;
        };
        /** @description Best result from a Phase 9 real-dynamics optimization search. For GA/PSO: a single-leg trajectory with real departure, heliocentric coast, and optional arrival/capture leg. For MGA: a multi-leg gravity-assist trajectory with one DSM per leg, found via MGA-1DSM + DE/SHADE/MBH, then refined by multiple shooting (Phase 9k task 3) — arc/fitness/mga_dv_dsms_ms reflect the multiple-shooting-corrected trajectory, not the raw search result. MGA-specific fields (mga_body_sequence, mga_dv_dsms_ms, mga_dsm_positions_m, mga_leg_tofs_days, mga_phase1_history, mga_ms_converged) are populated for MGA and null for GA/PSO. */
        OptimizeApiResult: {
            /**
             * @description Short code, matches optimization.method. (MultipleShooting is recognized in config but not implemented — the job fails before producing a result.)
             * @enum {string}
             */
            method: "GA" | "PSO" | "MGA";
            /** @description Human-readable label, e.g. "Genetic Algorithm (real dynamics)". */
            method_display: string;
            /** @description Human-readable label of optimization.objective, e.g. "Minimize ΔV". */
            objective: string;
            dep_offset_days: number;
            dep_jd: number;
            /** @description True anomaly of the departure burn at the parking orbit [rad] — one of the four search parameters. */
            theta_burn_rad: number;
            /** @description Departure burn magnitude [m/s] — the search parameter itself, not derived from any Lambert solve. */
            dv_departure_ms: number;
            /** @description Out-of-plane angle of the departure burn [rad] — handles the real plane change to the target's orbit. In Launch departure mode (Phase 14b) this slot is the asymptote's DLA [rad] and theta_burn_rad its RLA [rad]; dv_departure_ms is then the tangential injection burn the launch geometry derives (the gene itself is the v∞). */
            phi_out_of_plane_rad: number;
            /**
             * @description [trajectory.departure].mode (Phase 14a/14d). ParkingOrbit: the spacecraft starts in a given orbit and the departure burn is its own (free burn genes). Launch: the mission starts from a launch site, the launcher's upper stage delivers the injection (departure = launcher pool), and the search's departure genes are the asymptote (v∞, RLA, DLA) with the parking orbit/injection derived by closed-form launch geometry.
             * @enum {string}
             */
            departure_mode?: "ParkingOrbit" | "Launch";
            /**
             * @description Who pays dv_departure_ms — dv_ledger.departure_dv_pool surfaced at top level so a Phase 03 consumer can set external_stage on the departure burn without digging into the ledger. Null only when dv_ledger is.
             * @enum {string|null}
             */
            departure_dv_pool?: "launcher" | "onboard" | "split" | null;
            /** @description Epoch of the injection burn [JD] — equals dep_jd (the departure fires at the departure epoch; the parking-orbit coast in pre_departure_orbit_arc precedes it). */
            injection_epoch_jd?: number;
            /** @description Launch departure mode only (Phase 14c/14d): closed-form launch geometry for this result's own departure asymptote — RLA/DLA, the site-feasible plane, launch azimuth, coast angle and the exact injection state (MANUAL.md §13.6). Null in ParkingOrbit mode. A schematic ascent drawn from it is display only — pre_departure_orbit_arc stays the physical parking-orbit coast and is what Phase 03 references. */
            launch_geometry?: components["schemas"]["LaunchGeometryApiResult"] | null;
            /** @description Real time spent escaping the departure body's SOI [s]. arc's first points with t_s <= escape_duration_s are the departure-body-centered escape leg; everything after is the heliocentric cruise leg — useful for a frontend camera stage that zooms in on departure, then out for cruise, then in again on arrival. */
            escape_duration_s: number;
            /** @description Time of mission completion [days]: the capture burn's time if one was needed and achieved, else the time of closest approach to the target (the natural "arrival" moment for a Flyby). An output of the search, not a prescribed input — contrast the narrowing stage's tof_days, which is a chosen Lambert parameter. */
            achieved_tof_days: number;
            /** @description Real arrival/capture burn [m/s] — a tangential insertion burn at trajectory.capture.target_orbit_radius_m, computed from whatever real incoming trajectory the search actually produced (not separately targeted). 0.0 for a Flyby mission (no capture burn exists to charge). */
            dv_arrival_ms: number;
            /** @description Absolute time of the capture burn [s since departure], if one was needed and achieved. null for a Flyby. */
            capture_time_s?: number | null;
            /** @description Real inertial ΔV VECTOR for the capture burn [m/s] -- a tangential (retrograde) insertion burn opposing the real incoming relative-velocity direction, magnitude exactly dv_arrival_ms. null for a Flyby, or for MGA (not computed there yet), or any candidate that didn't achieve a real capture. */
            capture_dv_inertial_mps?: number[] | null;
            /** @description Real inertial ΔV VECTOR for the DEPARTURE burn [m/s] — the departure-side mirror of capture_dv_inertial_mps, so Phase 03 can prepend pre_departure_orbit_arc to the cruise reference and actually FIRE the departure at t_s = arc[0].t_s instead of treating the parking orbit as visual-only. As a velocity difference it needs no body-relative-to-inertial frame conversion. GA/PSO: post-burn minus pre-burn state from the exact circular_orbit_burn_state construction the search used — |v| equals dv_departure_ms by construction. MGA: the tangential periapsis escape burn, v0_hat * dv_escape from mga_departure_injection_state. null only when the departure body doesn't resolve (mirrors pre_departure_orbit_arc). */
            departure_dv_inertial_mps?: number[] | null;
            /** @description Arrival-side mirror of theta_burn_rad — true anomaly of the capture burn. null for a Flyby or any candidate that didn't achieve a real capture. */
            theta_arr_rad?: number | null;
            /** @description Arrival-side mirror of phi_out_of_plane_rad. null for a Flyby or any candidate that didn't achieve a real capture. */
            phi_arr_rad?: number | null;
            /** @description Dimensionless, normalized: ΔV/Hohmann-reference for MinDeltaV, achieved-TOF/Hohmann-reference for MinTof, |closest_approach - target_orbit_radius_m| / target_orbit_radius_m for MatchTargetDistance. No longer mixes raw units. For MGA (method = "MGA"): total ΔV [m/s] (departure + DSMs + arrival) AFTER the multiple-shooting refinement (Phase 9k task 3) — see mga_ms_converged. */
            fitness: number;
            /** @description Real closest-approach distance to the target body over the whole arc (departure leg + cruise leg + arrival leg) [km] — not just the distance at some prescribed end point. The narrowing stage's GA/PSO (OptimizerApiResult) has no equivalent of this field — its Lambert-only arc always ends exactly on target by construction, so it cannot miss. */
            miss_km: number;
            /** @description Target body's real heliocentric position at the moment of closest approach (or capture) [m] — plot this alongside arc's last point to visualize the miss distance. For MGA (method = "MGA"): computed as the final body's real ANISE-queried heliocentric position at dep_jd + achieved_tof_days (Phase 9k-v bug #1 fix — this was hardcoded to [0,0,0] for every MGA result before, which put the target body on top of the Sun in the frontend's 3D view). */
            target_r_arr_m: number[];
            /** @description Best-fitness-so-far per generation (GA) or iteration (PSO) — phase 2's (real-objective) history for GA. Same units as phase 2 in OptimizeStepMsg, NOT the same scale as phase1_convergence_km. */
            convergence: number[];
            /** @description Phase 1's (flyby-only) best-closest-approach-so-far history [km], one entry per generation across all restarts combined. Empty for PSO (no phase-1 split). */
            phase1_convergence_km: number[];
            /** @description The EXACT natural-units series the live convergence plot drew, persisted for reload/preset rendering — one [step, phase, value] row per sentinel-free streamed step: step on the unified budget-bounded generation counter, phase 1/2/3 (3 = the post-search refinement entries including the final polished value), value in the objective's natural units (MinDeltaV m/s, MinTof days, MatchTargetDistance km). The old `convergence` field is the internal normalized phase-2-only fitness history — a DIFFERENT series; prefer this one for display. Empty for MGA and for results saved before this field existed. */
            objective_history?: number[][];
            /** @description GA: every evaluated individual across both phases. PSO: empty. MGA (Phase 9k task 5): the best-so-far chromosome per generation — one row per phase1_history/convergence entry (MGA has no per-individual population exposed here, only DE/SHADE/MBH's running best) — generation is 0-indexed WITHIN each phase (unlike GA's population_log, whose generation matches the live-streamed OptimizeStepMsg.step value globally across restarts). The raw data for an x/y scatter of any two search parameters, colored by generation. */
            population_log: components["schemas"]["PopulationLogRow"][];
            /** @description Real propagated trajectory at the best point. For GA/PSO: departure leg + heliocentric cruise, under the configured force_model; leg_idx is null on all points. For MGA (Phase 9k task 3): the multiple-shooting-refined arc (real N-body perturbers via build_mga_perturber_entries, Newton/LM-corrected DSMs and flyby geometry — converged or best-effort, see mga_ms_converged) — heliocentric, all points labelled central_body="Sun", leg_idx carries the 0-indexed leg number. Falls back to the uncorrected Dopri5 repropagation (or the Keplerian/Lambert arc) only if the multiple-shooting refinement itself errors out (rare). */
            arc: components["schemas"]["ArcApiPoint"][];
            /** @description MGA only: full body-name sequence, e.g. ["Earth","Venus","Jupiter"]. Length = number of legs + 1. null for GA/PSO. */
            mga_body_sequence?: string[] | null;
            /** @description MGA only: per-leg DSM ΔV magnitudes [m/s], AFTER the multiple-shooting refinement (Phase 9k task 3) when it succeeded — falls back to the uncorrected search-time DSM magnitudes if the refinement errored out. Length = number of legs. null for GA/PSO. */
            mga_dv_dsms_ms?: number[] | null;
            /** @description MGA only: real inertial ΔV VECTORS for each leg's DSM [m/s] — mga_dv_dsms_inertial_mps[k].norm == mga_dv_dsms_ms[k] by construction. Same source/fallback convention as mga_dv_dsms_ms (multiple-shooting-corrected when available, else freshly evaluated from the uncorrected search-time chromosome) — what a downstream consumer actually fires as a cruise_seed.planned_burns entry, instead of reconstructing a direction client-side. null for GA/PSO, or if the fallback evaluation itself fails. */
            mga_dv_dsms_inertial_mps?: number[][] | null;
            /** @description MGA only: heliocentric [x, y, z] position [m] of each leg's DSM burn — the uncorrected search-time position (multiple shooting corrects the DSM's ΔV vector, not a separately-reported position). Length = number of legs. Use alongside mga_body_sequence to locate burn markers between body encounters. null for GA/PSO. */
            mga_dsm_positions_m?: number[][] | null;
            /** @description MGA only: per-leg DSM epochs [s since departure, same clock as arc's t_s] — the missing epoch half of mga_dv_dsms_inertial_mps, so Phase 03 can populate cruise_seed.planned_burns without inferring epochs by matching mga_dsm_positions_m against arc. mga_dsm_epochs_s[k] = (leg k's start + eta_k * tof_k) in seconds; pairs index-for-index with mga_dv_dsms_inertial_mps / mga_dsm_positions_m. Valid for the multiple-shooting-refined arc too — MS's free variables are DSM ΔVs and node states, never the leg timing, and its transit-time offsets cancel algebraically at the DSM instant. null for GA/PSO. */
            mga_dsm_epochs_s?: number[] | null;
            /** @description MGA only: per-leg time-of-flight [days]. Length = number of legs. Sum equals achieved_tof_days. null for GA/PSO. */
            mga_leg_tofs_days?: number[] | null;
            /** @description MGA only: phase-1 (DSM-only fitness, m/s sum of all DSM ΔVs) best-fitness history across all DE restarts. Distinct scale from convergence (phase 2, total ΔV). null for GA/PSO. */
            mga_phase1_history?: number[] | null;
            /** @description MGA only (Phase 9k task 3): whether the multiple-shooting refinement that produced arc/fitness/mga_dv_dsms_ms converged (Newton-Raphson within MAX_MS_ITER iterations) or is a best-effort, not-fully-corrected result — the refinement always returns SOME arc either way, this just says how much to trust it. null for GA/PSO. Also null for MGA if the refinement itself errored out entirely (rare — e.g. the winning chromosome's initial state was infeasible), in which case arc/mga_dv_dsms_ms fall back to the uncorrected search-time values instead. */
            mga_ms_converged?: boolean | null;
            /** @description MGA only (user-requested): the real ΔV [m/s] each intermediate flyby actually delivered, measured directly from the converged, real-dynamics-propagated trajectory -- |v_helio_at_SOI_exit - v_helio_at_SOI_entry| (not an analytic re-derivation from v-infinity-in/v-infinity-out; propagate's output is always in one consistent heliocentric frame regardless of which body was internally central during integration). Length = number of intermediate flyby bodies (mga_body_sequence minus departure/target). null under the same conditions as post_capture_orbit_arc: not MGA, or mga_ms_converged is not true -- no genuinely converged flyby passage to measure. */
            mga_flyby_dv_gained_ms?: number[] | null;
            /** @description Paired with mga_flyby_dv_gained_ms: real heliocentric speed [m/s] just before each flyby's real SOI entry. Compare against mga_flyby_speed_after_ms to see whether a given flyby added or removed heliocentric energy. Same length/gating as mga_flyby_dv_gained_ms. */
            mga_flyby_speed_before_ms?: number[] | null;
            /** @description Paired with mga_flyby_speed_before_ms: real heliocentric speed [m/s] just after each flyby's real SOI exit. speed_after > speed_before means that flyby ADDED heliocentric energy (taken from the planet's own orbit, by conservation of momentum); speed_after < speed_before means it REMOVED energy (given back to the planet) -- the real, checkable signature of an energy-shedding deceleration flyby (e.g. MESSENGER's resonant Mercury passes). Same length/gating as mga_flyby_dv_gained_ms. */
            mga_flyby_speed_after_ms?: number[] | null;
            /** @description Phase 12c, upgraded same day by 9y-h: the actual resulting orbit around the target body AFTER the capture burn — re-seeded with a post-burn velocity at the crossing/arrival point and propagated for 2 full orbital periods. Fixed (Phase 12k): points are now in the SAME heliocentric frame `arc` uses (the target body's own real heliocentric position at the capture epoch has already been added to every point) — this field used to return raw target-body-relative meters with `arc`'s heliocentric frame documented as separate/not-to-be-concatenated, which forced any consumer wanting one continuous path to drop these points rather than mix frames (confirmed as the real cause of a captured orbit reading as a flyby in a downstream replay that tried to build its reference trajectory from this data). central_body is still always the target body's own name (e.g. "Mars") — that field means "which body's gravity is locally dominant here," not "which frame this position is in." GA/PSO: the orbital plane is derived from the real propagated crossing state (r_rel × v_rel of the actual inbound trajectory). MGA: since 9y-h gave multiple shooting's final leg a real, non-degenerate target to converge onto (previously it targeted the arrival body's exact center), the orbital plane now also comes from a REAL propagated crossing state (`mga.rs::ms_arrival_relative_state`, re-propagating only the converged final leg) — the same construction as GA/PSO, not a modeling convention. Falls back to the pre-9y-h ecliptic-proxy convention (coplanar with the target body's own instantaneous heliocentric orbit) only in the rare case that real-state re-propagation itself fails despite mga_ms_converged being true. null for: any mission objective other than "Orbit"/"Landing" (Flyby has no capture burn; Rendezvous/SampleReturn aren't modeled as a periapsis-radius capture); an Orbit/Landing candidate that never achieved a real capture crossing; and — MGA only — whenever mga_ms_converged is false or null (building a resulting orbit on top of a physically-uncorrected or gap-filled trajectory would compound a bad foundation with a fabricated-looking orbit, so it's omitted rather than computed from the uncorrected arc). */
            post_capture_orbit_arc?: components["schemas"]["ArcApiPoint"][] | null;
            /** @description Orbital period [s] of post_capture_orbit_arc, computed analytically from the same inputs used to build the ring (not derived from the sampled points) -- so a frontend can show "period: X hours" directly instead of re-deriving it from raw geometry. Same presence/null gating as post_capture_orbit_arc itself (user-requested). */
            post_capture_orbit_period_s?: number | null;
            /** @description Periapsis radius [m] of post_capture_orbit_arc. For MGA this is the real configured/priced target_orbit_radius_m (or its floor-clamped value); same for single-leg. Equal to post_capture_orbit_apoapsis_m whenever post_capture_orbit_eccentricity is 0 (circular, the common case). */
            post_capture_orbit_periapsis_m?: number | null;
            /** @description Apoapsis radius [m] of post_capture_orbit_arc -- periapsis_m * (1+e)/(1-e). Equal to post_capture_orbit_periapsis_m when circular. */
            post_capture_orbit_apoapsis_m?: number | null;
            /** @description Eccentricity of post_capture_orbit_arc — derived from the REAL post-burn state: the real arrival velocity direction, speed scaled to the configured capture orbit's periapsis speed √(μ(1+e)/r) (Phase 14f: the single-leg GA/PSO/MBH path now honors [trajectory.capture].capture_eccentricity in BOTH the priced dv_arrival_ms/capture_dv_inertial_mps and this ring, as MGA and the survey already did). Equals the configured e for a tangential crossing; reports the real, slightly different shape when the crossing has a radial component. */
            post_capture_orbit_eccentricity?: number | null;
            /** @description Semi-major axis [m] of post_capture_orbit_arc (user-requested Kepler-element summary) — derived from the same real post-burn state the propagated ring itself was seeded with. */
            post_capture_orbit_sma_m?: number | null;
            /** @description Inclination [deg] of post_capture_orbit_arc's plane to the reference frame's fundamental plane (the J2000 frame every state in this pipeline is expressed in), acos(h_z/|h|) of the real post-burn state. */
            post_capture_orbit_inclination_deg?: number | null;
            /** @description Phase 12 (user-requested): the real parking orbit leading up to the departure burn, propagated for 2 full orbital periods, so the frontend can show where the spacecraft actually started instead of the arc beginning abruptly mid-escape. Fixed (Phase 12k): points are now in the SAME heliocentric frame `arc` uses (the departure body's own real heliocentric position at the departure epoch has already been added to every point) — previously returned as raw departure-body-relative meters, documented as a separate frame not to be concatenated with `arc`. central_body is still always the departure body's own name — that field means "which body's gravity is locally dominant here," not "which frame this position is in." GA/PSO uses the REAL parking orbit the search found (r_park_m/theta_burn_rad/the real orbital plane), ending exactly at the real burn point. MGA (`mga.rs::mga_departure_injection_state`) decodes the winning chromosome's already-optimized departure v∞ vector and constructs the real periapsis-injection state via the same closed-form hyperbolic-departure math GA/PSO uses — a real parking orbit at the correct radius/plane, not a stand-in. This is display-only: the search itself still has no committed burn-location/timing degree of freedom for MGA departure (12j, still open — `departure_escape_dv_ms` only prices a scalar ΔV magnitude). Always present when the departure body resolves (does not depend on mission objective or mga_ms_converged — every optimized mission has a departure leg). Purely additive/display — does not change any reported ΔV number. */
            pre_departure_orbit_arc?: components["schemas"]["ArcApiPoint"][] | null;
            /** @description Launch-vehicle check at THIS result's own departure C3 (Phase 14d-lite — the frontend used the survey's best_arc.launch_vehicle_check as a stand-in before). GA/PSO: C3 from the search's own injection state (|v_post|² − 2μ/r_park); MGA: from the chromosome's departure v∞ via mga_departure_injection_state. Null for a non-Earth departure, no [spacecraft].launch_vehicle, an unknown vehicle name, or a departure body that doesn't resolve. */
            launch_vehicle_check?: components["schemas"]["LaunchVehicleCheckApiResult"] | null;
            /** @description Two-pool ΔV ledger + propellant feasibility (Phase 14e) for this result: departure = dv_departure_ms (split launcher/onboard per launch_vehicle_check), DSMs = sum of mga_dv_dsms_ms (MGA) or 0, arrival = dv_arrival_ms. Null only when the departure body doesn't resolve. */
            dv_ledger?: components["schemas"]["DvLedgerApiResult"] | null;
        };
        /** @description One evaluated individual from a Phase 9 GA or MGA search (empty for PSO). Raw data behind an x/y-scatter-colored-by-generation plot. */
        PopulationLogRow: {
            /** @description 1 (flyby-only search), 2 (real-objective refinement), or 3 (post-search coordinate-descent refinement probes -- all logged at generation = the configured budget) — same meaning as OptimizeStepMsg.phase. */
            phase: number;
            /** @description GA: absolute generation index (phase 1's independent restarts and phase 2 share one continuously-increasing index, no overlap), matching the live-streamed OptimizeStepMsg.step value. MGA (Phase 9k task 5): 0-indexed WITHIN each phase instead — NOT globally aligned with OptimizeStepMsg.step, a documented deviation from GA's convention. */
            generation: number;
            /** @description GA: always 4 entries, [dep_offset_days, theta_burn_rad, dv_mps, phi_out_of_plane_rad]. MGA: the raw chromosome vector (see mga.rs's chromosome layout doc comment) — length varies with leg count (2 + 5*n_legs), NOT fixed at 4. */
            params: number[];
            /** @description This individual's fitness (km for phase 1, dimensionless normalized for phase 2 on GA; for MGA, phase 1 is the DSM-only sum in m/s and phase 2 is total ΔV in m/s — see OptimizeApiResult.mga_phase1_history/convergence). f64::MAX (~1.7976931348623157e+308) marks an infeasible individual — filter these out before plotting, same sentinel convention as OptimizeApiResult.fitness. */
            fitness: number;
            /** @description This individual's own arrival burn [m/s] (real crossing burn, or the capture-into-any-orbit burn at its closest approach; 0.0 for a Flyby mission) — recorded as a fitness-evaluation side-product for the outcome scatter plots (departure vs. arrival ΔV). null for an infeasible candidate, MGA rows, and rows from before this field existed. */
            dv_arrival_ms?: number | null;
            /** @description This individual's own achieved time of flight [days] (capture time, or closest-approach time when it never captured) — same outcome recording (departure offset vs. coast time scatter). null for an infeasible candidate, MGA rows, and rows from before this field existed. */
            tof_days?: number | null;
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    health: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Server is up */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthResponse"];
                };
            };
        };
    };
    getBodies: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Body catalog */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BodiesResponse"];
                };
            };
        };
    };
    getBodyState: {
        parameters: {
            query: {
                /** @description Same format as trajectory.departure_epoch, e.g. "2026-09-15T00:00:00 UTC" (trailing " UTC" optional) */
                epoch: string;
            };
            header?: never;
            path: {
                /** @description Body name, case-insensitive, must match GET /api/bodies catalog */
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Heliocentric state vector, SI units */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BodyStateResponse"];
                };
            };
            /** @description Missing or unparseable epoch query parameter */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Body name not found in catalog */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Body has no ANISE/DE440S ephemeris coverage */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description ANISE kernel failed to load or ephemeris query failed */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    getHardware: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Hardware catalog */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HardwareResponse"];
                };
            };
        };
    };
    getObjectives: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Objectives list */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ObjectivesResponse"];
                };
            };
        };
    };
    getPresets: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Preset list */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PresetsResponse"];
                };
            };
        };
    };
    validateConfig: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MissionConfig"];
            };
        };
        responses: {
            /** @description Validation result (valid or not) */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ValidationResponse"];
                };
            };
            /** @description JSON parse error */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    designTrajectory: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MissionConfig"];
            };
        };
        responses: {
            /** @description Trajectory design result */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TrajectoryApiResult"];
                };
            };
            /** @description JSON parse error */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Config validation errors */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorsResponse"];
                };
            };
            /** @description Solver error */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    designGnc: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MissionConfig"];
            };
        };
        responses: {
            /** @description GNC design result */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GncDesign"];
                };
            };
            /** @description JSON parse error */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Config or sizing error */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    designVehicle: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["VehicleRequest"];
            };
        };
        responses: {
            /** @description Derived vehicle properties */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["VehicleProperties"];
                };
            };
            /** @description JSON parse error */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    designSlewTest: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SlewTestRequest"];
            };
        };
        responses: {
            /** @description Slew-test result */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SlewTestResult"];
                };
            };
            /** @description JSON parse error */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Invalid parameter (e.g. unknown control_mode, initial_error_deg out of [0,180]) */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    startSimulate: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MissionConfig"];
            };
        };
        responses: {
            /** @description Job started */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["StartSimResponse"];
                };
            };
            /** @description JSON parse error */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Config validation errors */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorsResponse"];
                };
            };
        };
    };
    getSimulateStatus: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                job_id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Job status */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JobStatusResponse"];
                };
            };
            /** @description Job not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    streamSimulate: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                job_id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Switching Protocols — each subsequent text frame is JSON-serialized CruiseMcRunMsg (cruise MC job), CruiseStepMsg (cruise job), SimStepMsg (single-run job), or McRunMsg (Monte Carlo job), until a final "__DONE__" text frame */
            101: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Job not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    getSimulateSteps: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                job_id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Step or run history */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["StepsResponse"] | components["schemas"]["CruiseStepsResponse"] | components["schemas"]["McRunsResponse"] | components["schemas"]["CruiseMcRunsResponse"];
                };
            };
            /** @description Job not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    getSimulateResult: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                job_id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Cruise MC, cruise, simulation, or Monte Carlo result */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CruiseMcSummaryResult"] | components["schemas"]["CruiseResult"] | components["schemas"]["SimResult"] | components["schemas"]["McSummaryResult"];
                };
            };
            /** @description Job not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Job still running */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Job failed */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    cancelSimulate: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                job_id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Cancellation requested */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CancelResponse"];
                };
            };
            /** @description Job not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    startOptimize: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MissionConfig"];
            };
        };
        responses: {
            /** @description Job started */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["StartOptimizeResponse"];
                };
            };
            /** @description JSON parse error */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Config validation errors, or missing [optimization] section */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorsResponse"];
                };
            };
        };
    };
    getOptimizeStatus: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                job_id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Job status */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OptimizeJobStatusResponse"];
                };
            };
            /** @description Job not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    streamOptimize: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                job_id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Switching Protocols — each subsequent text frame is JSON-serialized OptimizeStepMsg or (MGA only) MgaSequenceContextMsg — distinguish by the presence of msg_type (MgaSequenceContextMsg always has it, OptimizeStepMsg never does) — until a final "__DONE__" text frame */
            101: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Job not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    getOptimizeResult: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                job_id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Optimization result */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OptimizeApiResult"];
                };
            };
            /** @description Job not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Job still running */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Job failed — e.g. method = "MultipleShooting" (not implemented yet), or no valid solution at the best point */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    cancelOptimize: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                job_id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Cancellation requested */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CancelResponse"];
                };
            };
            /** @description Job not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    searchMgaSequence: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MissionConfig"];
            };
        };
        responses: {
            /** @description Ranked sequence list */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SequenceSearchApiResult"];
                };
            };
            /** @description JSON parse error */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Config validation errors, or missing [optimization]/[optimization.mga]/[optimization.mga.sequence_search] section */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorsResponse"];
                };
            };
        };
    };
    startMgaScan: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MissionConfig"];
            };
        };
        responses: {
            /** @description Job started */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["StartMgaScanResponse"];
                };
            };
            /** @description JSON parse error */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Config validation errors, or missing [optimization.mga.scan] section */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorsResponse"];
                };
            };
        };
    };
    getMgaScanStatus: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                job_id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Job status */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MgaScanJobStatusResponse"];
                };
            };
            /** @description Job not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    getMgaScanResult: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                job_id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Scan result */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MgaScanApiResult"];
                };
            };
            /** @description Job not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Job still running */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Job failed */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
}
